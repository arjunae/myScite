diff -ur a/SciTEWin.cxx b/SciTEWin.cxx
--- a/SciTEWin.cxx	2025-04-16 10:50:02.547607500 +0200
+++ b/SciTEWin.cxx	2025-05-07 20:39:55.377418500 +0200
@@ -289,6 +290,24 @@
 		exit(FALSE);
 }
 
+void CleanupMenu(HMENU hMenu) {
+    int count = GetMenuItemCount(hMenu);
+    for (int i = 0; i < count; ++i) {
+        MENUITEMINFO mii = { sizeof(mii) };
+        mii.fMask = MIIM_FTYPE | MIIM_DATA | MIIM_SUBMENU;
+        GetMenuItemInfo(hMenu, i, TRUE, &mii);
+
+        if ((mii.fType & MFT_OWNERDRAW) && mii.dwItemData) {
+            delete reinterpret_cast<std::wstring*>(mii.dwItemData);
+        }
+
+        if (mii.hSubMenu) {
+            CleanupMenu(mii.hSubMenu);
+        }
+    }
+    DestroyMenu(hMenu);
+}
+
 static int CodePageFromName(const std::string &encodingName) {
 	struct Encoding {
 		const char *name;
@@ -2022,7 +2041,9 @@
 			QuitProgram();
 			return 1;
 
-		case WM_DESTROY:
+		
+		 case WM_DESTROY:
+			CleanupMenu(GetMenu(MainHWND()));
 			break;
 
 		case WM_SETTINGCHANGE:
@@ -2074,6 +2095,86 @@
 		case WM_COPYDATA:
 			return uniqueInstance.CopyData(reinterpret_cast<COPYDATASTRUCT *>(lParam));
 
+		case WM_MEASUREITEM: {
+			LPMEASUREITEMSTRUCT pmis = (LPMEASUREITEMSTRUCT)lParam;
+			if (pmis->CtlType == ODT_MENU) {
+				HDC hdc = CreateCompatibleDC(NULL);
+				std::wstring* pText = reinterpret_cast<std::wstring*>(pmis->itemData);
+				SIZE size;
+				GetTextExtentPoint32W(hdc, pText->c_str(), (int)pText->length(), &size);
+				pmis->itemWidth = size.cx + 16;
+				pmis->itemHeight = size.cy + 8;
+				return TRUE;
+			}
+			break;
+	   }	
+	   
+	   case WM_DRAWITEM: {
+			LPDRAWITEMSTRUCT pdis = (LPDRAWITEMSTRUCT)lParam;
+			if (pdis->CtlType == ODT_MENU) {
+				std::wstring* pText = reinterpret_cast<std::wstring*>(pdis->itemData);
+				std::wstring label, shortcut;
+				size_t tabPos = pText->find(L'\t');
+				if (tabPos != std::wstring::npos) {
+					label = pText->substr(0, tabPos);
+					shortcut = pText->substr(tabPos + 1);
+				} else {
+					label = *pText;
+				}
+
+				// Set background color based on item state
+				COLORREF bgColor = (pdis->itemState & ODS_SELECTED) ? GetSysColor(COLOR_HIGHLIGHT) : GetSysColor(COLOR_MENU);
+				HBRUSH hBrush = CreateSolidBrush(bgColor);
+				FillRect(pdis->hDC, &pdis->rcItem, hBrush);
+				DeleteObject(hBrush);
+
+				// Set text color
+				COLORREF textColor = (pdis->itemState & ODS_SELECTED) ? GetSysColor(COLOR_HIGHLIGHTTEXT) : GetSysColor(COLOR_MENUTEXT);
+				SetTextColor(pdis->hDC, textColor);
+				SetBkMode(pdis->hDC, TRANSPARENT);
+
+				// Font settings (change these to customize font size and type)
+				int fontSize = 9;  // Set desired font size
+				std::wstring fontFamily = L"Segoe UI";  // Set desired font family
+
+				// Create a font with the specified size and type
+				LOGFONT logFont = {0};
+				logFont.lfHeight = -MulDiv(fontSize, GetDeviceCaps(pdis->hDC, LOGPIXELSY), 72);  // Height in logical units
+				wcscpy_s(logFont.lfFaceName, fontFamily.c_str());
+				HFONT hFont = CreateFontIndirect(&logFont);
+				
+				// Select the new font
+				HFONT hOldFont = (HFONT)SelectObject(pdis->hDC, hFont);
+
+				// Calculate positions (with padding)
+				RECT rcLabel = pdis->rcItem;
+				RECT rcShortcut = pdis->rcItem;
+				int padding = 20;  // Padding between entries, adjust this value as needed
+
+				// Measure shortcut text width
+				SIZE sizeShortcut = {0};
+				if (!shortcut.empty()) {
+					GetTextExtentPoint32W(pdis->hDC, shortcut.c_str(), (int)shortcut.length(), &sizeShortcut);
+					rcShortcut.left = rcShortcut.right - sizeShortcut.cx - padding;
+					rcLabel.right = rcShortcut.left - padding;
+				}
+
+				// Draw label
+				DrawTextW(pdis->hDC, label.c_str(), -1, &rcLabel, DT_SINGLELINE | DT_LEFT | DT_VCENTER);
+
+				// Draw shortcut
+				if (!shortcut.empty()) {
+					DrawTextW(pdis->hDC, shortcut.c_str(), -1, &rcShortcut, DT_SINGLELINE | DT_RIGHT | DT_VCENTER);
+				}
+
+				// Restore the original font
+				SelectObject(pdis->hDC, hOldFont);
+				DeleteObject(hFont);  // Don't forget to delete the font after usage!
+
+				return TRUE;
+			}
+			break;
+		}
 		default:
 			return ::DefWindowProcW(MainHWND(), iMessage, wParam, lParam);
 		}
diff -ur a/SciTEWinBar.cxx b/SciTEWinBar.cxx
--- a/SciTEWinBar.cxx	2025-04-16 10:50:02.549603700 +0200
+++ b/SciTEWinBar.cxx	2025-05-07 19:47:18.751666600 +0200
@@ -642,46 +642,217 @@
 	::SendMessage(hwndToolBar, TB_ADDBUTTONS, barbuttons.GetSize(), reinterpret_cast<LPARAM>(tbb));
 	delete []tbb;
 	CheckMenus();
-
 }
 
 void SciTEWin::SetMenuItem(int menuNumber, int position, int itemID,
                            const GUI::gui_char *text, const GUI::gui_char *mnemonic) {
-	// On Windows the menu items are modified if they already exist or are created
-	HMENU hmenu = ::GetSubMenu(::GetMenu(MainHWND()), menuNumber);
-	GUI::gui_string sTextMnemonic = text;
-	long keycode = 0;
-	if (mnemonic && *mnemonic) {
-		keycode = SciTEKeys::ParseKeyCode(GUI::UTF8FromString(mnemonic).c_str());
-		if (keycode) {
-			sTextMnemonic += GUI_TEXT("\t");
-			sTextMnemonic += mnemonic;
-		}
-		// the keycode could be used to make a custom accelerator table
-		// but for now, the menu's item data is used instead for command
-		// tools, and for other menu entries it is just discarded.
-	}
-
-	const UINT typeFlags = (text[0]) ? MF_STRING : MF_SEPARATOR;
-	if (::GetMenuState(hmenu, itemID, MF_BYCOMMAND) == (UINT)(-1)) {
-		// Not present so insert
-		::InsertMenuW(hmenu, position, MF_BYPOSITION | typeFlags, itemID, sTextMnemonic.c_str());
-	} else {
-		::ModifyMenuW(hmenu, itemID, MF_BYCOMMAND | typeFlags, itemID, sTextMnemonic.c_str());
-	}
+    HMENU hmenu = ::GetSubMenu(::GetMenu(MainHWND()), menuNumber);
+    if (!hmenu) {
+        std::cout << "Failed to get submenu at index " << menuNumber << std::endl;
+        return;
+    }
+
+    GUI::gui_string sTextMnemonic = text;
+    long keycode = 0;
+
+    if (mnemonic && *mnemonic) {
+        keycode = SciTEKeys::ParseKeyCode(GUI::UTF8FromString(mnemonic).c_str());
+        if (keycode) {
+            sTextMnemonic += GUI_TEXT("\t");
+            sTextMnemonic += mnemonic;
+            std::wcout << L"Mnemonic added: " << sTextMnemonic.c_str() << std::endl;
+        }
+    }
+
+    // Convert to wide string
+    std::wstring* pText = new std::wstring(sTextMnemonic);
+    std::wcout << L"Creating menu item: [" << *pText << L"], ID = " << itemID << std::endl;
+
+    MENUITEMINFOW mii = { sizeof(mii) };
+    mii.fMask = MIIM_ID | MIIM_FTYPE | MIIM_DATA;
+    mii.fType = MFT_OWNERDRAW;
+    mii.wID = itemID;
+    mii.dwItemData = reinterpret_cast<ULONG_PTR>(pText);
+
+    UINT state = ::GetMenuState(hmenu, itemID, MF_BYCOMMAND);
+    if (state == (UINT)(-1)) {
+        // Not present — insert
+        if (::InsertMenuItemW(hmenu, position, TRUE, &mii)) {
+            std::cout << "Inserted new menu item at position " << position << std::endl;
+        } else {
+            std::cout << "Failed to insert menu item. Error: " << GetLastError() << std::endl;
+        }
+    } else {
+        // Modify existing — clean up old item data
+        MENUITEMINFOW oldMii = { sizeof(oldMii) };
+        oldMii.fMask = MIIM_DATA;
+        if (::GetMenuItemInfoW(hmenu, itemID, FALSE, &oldMii)) {
+            std::wstring* oldText = reinterpret_cast<std::wstring*>(oldMii.dwItemData);
+            std::wcout << L"Replacing existing item: Old text = " << *oldText << std::endl;
+            delete oldText;
+        } else {
+            std::cout << "Failed to get existing item info for modification. Error: " << GetLastError() << std::endl;
+        }
+
+        if (::SetMenuItemInfoW(hmenu, itemID, FALSE, &mii)) {
+            std::cout << "Modified existing menu item with new text." << std::endl;
+        } else {
+            std::cout << "Failed to modify menu item. Error: " << GetLastError() << std::endl;
+        }
+    }
+
+    if (itemID >= IDM_TOOLS && itemID < IDM_TOOLS + toolMax) {
+        // Keycode stashing optional — here we just log
+        std::cout << "Tool menu item, keycode = " << keycode << std::endl;
+    }
+}
+
+
+struct MenuItem {
+    std::string label;    // e.g. "&New"
+    std::string shortcut; // e.g. "Ctrl+N" (from "\\tCtrl+N")
+    std::string id;       // e.g. "IDM_NEW" or "SEPARATOR"
+    int         level;    // 0 = top‐level popup, 1 = first submenu, etc.
+    bool        isSubMenu;// true if this entry is a POPUP header
+    bool        isTopLevel;// true if this POPUP is directly under the menu‐bar
+};
+
+// Helper to trim whitespace from both ends
+std::string trim(const std::string& s) {
+    auto start = s.find_first_not_of(" \t\r\n");
+    auto end = s.find_last_not_of(" \t\r\n");
+    if (start == std::string::npos || end == std::string::npos)
+        return "";
+    return s.substr(start, end - start + 1);
+}
+
+std::vector<MenuItem> loadMenuFromRc(const std::string& filename) {
+    std::vector<MenuItem> menuItems;
+    std::ifstream file(filename);
+    if (!file.is_open()) {
+        std::cerr << "Error: Cannot open file: " << filename << "\n";
+        return menuItems;
+    }
+
+    std::string line;
+    int currentLevel = 0;
+
+    while (std::getline(file, line)) {
+        line = trim(line);
+        if (line.empty() || line[0] == ';')
+            continue;
+
+        // --- POPUP headers (no level change yet) ---
+        if (line.rfind("POPUP", 0) == 0) {
+            auto firstQ = line.find('\"');
+            auto lastQ  = line.rfind('\"');
+            std::string raw = (firstQ != std::string::npos && lastQ > firstQ)
+                              ? line.substr(firstQ + 1, lastQ - firstQ - 1)
+                              : "";
+
+            bool isTop = (currentLevel == 0);
+            menuItems.push_back({ raw, "", "", currentLevel, true, isTop });
+            continue;
+        }
+
+        // Actual block entry increases nesting
+        if (line == "BEGIN") {
+            ++currentLevel;
+            continue;
+        }
+
+        // Match any "END" at start (catches "END // ...")
+        if (line.rfind("END", 0) == 0) {
+            if (currentLevel > 0) --currentLevel;
+            continue;
+        }
+
+        // --- Separator (prefix match to allow commas/comments) ---
+        if (line.rfind("MENUITEM SEPARATOR", 0) == 0) {
+            menuItems.push_back({ "", "", "SEPARATOR", currentLevel, false, false });
+            continue;
+        }
+
+        // --- Regular menu items ---
+        if (line.rfind("MENUITEM", 0) == 0) {
+            auto firstQ = line.find('\"');
+            auto commaQ = line.find("\",", firstQ + 1);
+            if (firstQ == std::string::npos || commaQ == std::string::npos)
+                continue;
+
+            std::string raw    = line.substr(firstQ + 1, commaQ - firstQ - 1);
+            std::string idPart = trim(line.substr(commaQ + 2));
+
+            std::string label, shortcut;
+            auto tpos = raw.find("\\t");
+            if (tpos != std::string::npos) {
+                label    = raw.substr(0, tpos);
+                shortcut = raw.substr(tpos + 2);
+            } else {
+                label = raw;
+            }
+
+            menuItems.push_back({ label, shortcut, idPart, currentLevel, false, false });
+        }
+    }
+
+    file.close();
+    return menuItems;
+}
+
+void ReplaceMenu(HWND hwnd, const std::vector<MenuItem>& items) {
+    HMENU hMenuBar = CreateMenu();
+    HMENU currTop = nullptr;
+    HMENU currSub = nullptr;
+
+    for (const auto& mi : items) {
+        std::wstring text(mi.label.begin(), mi.label.end());
+
+        if (mi.isTopLevel) {
+            currTop = CreatePopupMenu();
+            AppendMenuW(hMenuBar, MF_POPUP, (UINT_PTR)currTop, text.c_str());
+            currSub = nullptr;
+        }
+        else if (mi.isSubMenu) {
+            if (!currTop) continue;
+            currSub = CreatePopupMenu();
+            AppendMenuW(currTop, MF_POPUP, (UINT_PTR)currSub, text.c_str());
+        }
+        else {
+            HMENU target = currSub ? currSub : currTop;
+            if (!target) continue;
+
+            if (mi.id == "SEPARATOR") {
+                AppendMenuW(target, MF_SEPARATOR, 0, nullptr);
+            }
+            else {
+                // Owner-drawn menu item
+                UINT cmd = SciTEBase::GetMenuCommandAsInt(mi.id);
+
+                MENUITEMINFOW mii = { sizeof(mii) };
+                mii.fMask = MIIM_ID | MIIM_FTYPE | MIIM_DATA;
+                mii.fType = MFT_OWNERDRAW;
+                mii.wID = cmd;
+
+                // Combine label and shortcut with a tab character
+                std::wstring fullText = text;
+                if (!mi.shortcut.empty()) {
+                    fullText += L"\t" + std::wstring(mi.shortcut.begin(), mi.shortcut.end());
+                }
+
+                std::wstring* pText = new std::wstring(fullText);
+                mii.dwItemData = reinterpret_cast<ULONG_PTR>(pText);
+
+                InsertMenuItemW(target, GetMenuItemCount(target), TRUE, &mii);
+            }
+        }
+    }
 
-	if (itemID >= IDM_TOOLS && itemID < IDM_TOOLS + toolMax) {
-		// Stow the keycode for later retrieval.
-		// Do this even if 0, in case the menu already existed (e.g. ModifyMenu)
-		MENUITEMINFO mii;
-		mii.cbSize = sizeof(MENUITEMINFO);
-		mii.fMask = MIIM_DATA;
-		mii.dwItemData = keycode;
-		::SetMenuItemInfo(hmenu, itemID, FALSE, &mii);
-	}
+    SetMenu(hwnd, hMenuBar);
+    DrawMenuBar(hwnd);
+}
 
 
-}
 
 void SciTEWin::RedrawMenu() {
 	// Make previous change visible.
@@ -984,6 +1155,7 @@
 	return retResult;
 }
 
+
 /**
  * Create all the needed windows.
  */
@@ -1204,6 +1376,9 @@
 	bands.push_back(Band(true, replaceStrip.Height(), false, replaceStrip));
 	bands.push_back(Band(true, heightStatus, false, wStatusBar));
 
+ 	// Replace the static Menu from SciteRes.rc
+	std::vector<MenuItem> menuItems = loadMenuFromRc("SciTEMenu.rc");
+	ReplaceMenu(MainHWND(), menuItems);
 #ifndef NO_LUA
 		if (props.GetExpandedString("ext.lua.startup.script").length() == 0)
 			DestroyMenuItem(menuOptions,IDM_OPENLUAEXTERNALFILE);
@@ -1211,3 +1386,5 @@
 		DestroyMenuItem(menuOptions,IDM_OPENLUAEXTERNALFILE);
 #endif
 }
+
+
diff -ur a/SciteWin.h b/SciteWin.h
--- a/SciteWin.h	2025-04-16 10:50:02.576394200 +0200
+++ b/SciteWin.h	2025-05-07 18:49:11.903964900 +0200
@@ -7,6 +7,9 @@
 
 #include <stdlib.h>
 #include <string.h>
+#include <fstream>
+#include <sstream>
+#include <iostream>
 #include <ctype.h>
 #include <stdio.h>
 #include <fcntl.h>
