##
# Javascript API, as fetched from MSDN. 
# imAugust2016, Marcedo@HabmalneFrage.de | https://sourceforge.net/projects/scite-webdev
##

# Javascripts Object Names
ActiveXObject(newObj,servername,typename,location)Enables and returns a reference to an Automation object.\t\nnewObj = new ActiveXObject(servername.typename[, location])
GetObject(pathname,class,appname,objectype)Returns a reference to an Automation object from a file.\t\nGetObject([pathname] [, class])
Array(arrayObj,size,element0,...,elementN)Provides support for creation of arrays of any data type.\t\narrayObj = new Array([element0[, element1[, ...[, elementN]]]])
ArrayBuffer(arrayBuffer,length)Represents a raw buffer of binary data, which is used to store data for the different typed arrays.\t\nArrayBuffers cannot be read from or written to directly, but can be passed to a typed array\t\nor DataView Object to interpret the raw buffer as needed.\t\n arrayBuffer = new ArrayBuffer(length);
Boolean(boolObj,boolValue)Creates a new Boolean value.\t\n boolObj = new Boolean([boolValue])
DataView(dataView,buffer,byteOffset,byteLength)You can use a DataView object to read and write\t\nthe different kinds of binary data to any location in the ArrayBuffer.\t\n dataView = new DataView(buffer, byteOffset, byteLength);
Date(dateObj,dateVal,year,month,date,hours,minutes,seconds,ms)Enables basic storage and retrieval of dates and times.\t\ndateObj = new Date(year, month, date[, hours[, minutes[, seconds[,ms]]]]) 
Debug()An intrinsic global object that sends output to a debugger.\t\n Debug.function
Debug()Debug constants return constant values that are properties\t\nof the Debug object.
Enumerator(enumObj,collection)Enables enumeration of items in a collection.\t\nenumObj = new Enumerator([collection]) 
Error(errorObj,number,description)Contains information about errors.\t\nerrorObj = new Error([number[, description]])
Float32Array(float32Array,length,array,buffer,byteOffset,length)A typed array of 32-bit float values. The contents are initialized to 0.\t\n\t\n float32Array = new Float32Array( length );\t\nfloat32Array = new Float32Array( array );\t\nfloat32Array = new Float32Array( buffer, byteOffset, length);
Float64Array(float64Array,length,array,buffer,byteOffset,length)A typed array of 64-bit float values. The contents are initialized to 0.\t\n\t\n float64Array = new Float64Array( length );\t\nfloat64Array = new Float64Array( array );\t\nfloat64Array = new Float64Array( buffer, byteOffset, length);
Function(functionName,argname1...argnameN,body)Creates a new function.\t\nfunctionName = new Function( [argname1,  [... argnameN,]] body );
GetObject(pathname,class,appname,objectype)Returns a reference to an Automation object from a file.\t\nGetObject([pathname] [, class])
Global()An intrinsic object whose purpose is to collect global functions and constants into one object.
HTML()You can use HTML tag methods to place HTML elements around text in a String object.
Infinity()A number that is larger than the largest floating point number. Negative Infinity (-Infinity) is smaller than the smallest floating point number.\t\n Infinity 
Int16Array(int16Array,length,array,buffer,byteOffset,length)A typed array of 16-bit integer values. The contents are initialized to 0.\t\n int16Array = new Int16Array( length );\t\nint16Array = new Int16Array( array );\t\nint16Array = new Int16Array( buffer, byteOffset, length);
Int32Array(int32Array,length,array,buffer,byteOffset,length)A typed array of 32-bit integer values. The contents are initialized to 0.\t\n int32Array = new Int32Array( length );\t\nint32Array = new Int32Array( array );\t\nint32Array = new Int32Array( buffer, byteOffset, length);
Int8Array(int8Array,length,array,buffer,byteOffset,length)A typed array of 8-bit integer values. The contents are initialized to 0.\t\n\t\n int8Array = new Int8Array( length );\t\nintArray = new Int8Array( array );\t\nintArray = new Int8Array( buffer, byteOffset, length);
Json(Method)An intrinsic object that provides functions to convert JavaScript values to and from the JavaScript Object Notation (JSON) format.\t\nThe JSON.stringify function serializes a JavaScript value to JSON text. The JSON.parse function deserializes JSON text to produce a JavaScript value.\t\n JSON.[method]
Map()A collection of key/value pairs.\t\n mapObj = new Map()
Math()Math constants return constant values\t\nthat are properties of the Math object.\t\n var radius = 3,\t\nvar area = Math.PI  radius  radius,\t\ndocument.write(area);\t\n\t\n// Output: 28.274333882308138
Math(property,method)An intrinsic object that provides basic mathematics functionality\t\nand constants.\t\n Math.[{property | method}]
Number()The following number constants are properties of the Number object.
Number(numObj,value)An object that represents a number of any kind.\t\nAll JavaScript numbers are 64-bit floating-point numbers.\t\n numObj = new Number(value)
Object(obj,value)Provides functionality common to all JavaScript objects.\t\n obj = new Object([value]) 
Promise(promise,resolve,reject)Provides a mechanism to schedule work to be done on a value that has not yet been computed.\t\nIt is an abstraction for managing interactions with asynchronous APIs.\t\n var promise = new Promise(function(resolve, reject) { ... });
Proxy(target,handler)Enables custom behavior for an object.\t\n proxyObj = new Proxy(target, handler)
Reflect(method)Provides methods for use in operations that are intercepted.\t\n Reflect.[method]
RegExp()An intrinsic global object that stores information about the\t\nresults of regular expression pattern matches.\t\n RegExp.property 
Regular(re,pattern,flags)An object that contains a regular expression pattern along\t\nwith flags that identify how to apply the pattern.
ScriptEngine()Gets the name of the scripting language in use.\t\n ScriptEngine()
ScriptEngineBuildVersion()Gets the build version number of the scripting engine in use.\t\n ScriptEngineBuildVersion()
ScriptEngineMajorVersion()Gets the major version number of the scripting engine in use.\t\n ScriptEngineMajorVersion()
ScriptEngineMinorVersion()Gets the minor version number of the scripting engine in use.\t\n ScriptEngineMinorVersion()
Set()A collection of unique values that may be of any type.\t\n setObj = new Set() 
String(newString,stringLiteral)Allows manipulation and formatting of text strings and determination\t\nand location of substrings within strings.\t\n newString = new String(["stringLiteral"])
Symbol(desc)Allows you to create a unique identifier.\t\n obj = Symbol(desc)
Uint16Array(uint16Array,length,array,buffer,byteOffset,length)A typed array of 16-bit unsigned integer values. The contents are initialized to 0. \t\n uint16Array = new Uint16Array( length );\t\nuint16Array = new Uint16Array( array );\t\nuint16Array = new Uint16Array( buffer, byteOffset, length );
Uint32Array(uint32Array,length,array,buffer,byteOffset,length)A typed array of 32-bit unsigned integer values. The contents are initialized to 0. \t\n uint32Array = new Uint32Array( length );\t\nuint32Array = new Uint32Array( array );\t\nuint32Array = new Uint32Array( buffer, byteOffset, length);
Uint8Array(uint8Array,length,array,buffer,byteOffset,length)A typed array of 8-bit unsigned integer values. The contents are initialized to 0. \t\n uint8Array = new Uint8Array( length );\t\nuint8Array = new Uint8Array( array );\t\nuint8Array = new Uint8Array( buffer, byteOffset, length);
Uint8ClampedArray(uint8ClampedArray,length,array,buffer,byteOffset,length)A typed array of 8-bit unsigned integers with values clamped to the range 0-255.\t\nThe contents are initialized to 0.\t\n uint8ClampedArray = new Uint8ClampedArray( length );\t\nuint8ClampedArray = new Uint8ClampedArray( array );\t\nuint8ClampedArray = new Uint8ClampedArray( buffer, byteOffset, length);
VBArray(varName,safeArray)Provides access to Visual Basic safe arrays.\t\nvarName = new VBArray(safeArray) 
WeakMap()A collection of key/value pairs in which each key is an object reference.\t\n weakmapObj = new WeakMap()
WeakSet()A collection of unique objects that may be of any type.\t\n setObj = new WeakSet()
WinRTError()When a Windows Runtime call returns an HRESULT that indicates a failure, JavaScript converts it to a special Windows Runtime error.\t\nIt is available only in Windows 8.x Store apps, when the Windows Runtime is available, as part of the global JavaScript namespace.\t\n errorObj = new WinRTError();
Array.concat(array1,item1,.,.,.,,itemN,)Combines two or more arrays.\t\n array1.concat([item1[, item2[, . . . [, itemN]]]]) 
Array.constructor()Specifies the function that creates an array.\t\n array.constructor
Array.entries(arrayObj)Returns an iterator that returns the key/value pairs of the array.\t\n arrayObj.entries();
Array.every()Determines whether all the members of an array satisfy the\t\nspecified test.\t\narray1.every(callbackfn[, thisArg])
Array.fill(arrayObj,value,start,end)Populates an array with a specified value.\t\n arrayObj.fill(value [ , start [ , end ] ]);
Array.filter()Returns the elements of an array that meet the condition specified\t\nin a callback function.\t\n array1.filter(callbackfn[, thisArg])
Array.findIndex(arrayObj,callbackfn,thisArg)Returns an index value for the first array element that meets\t\ntest criteria specified in a callback function.\t\n arrayObj.findIndex(callbackfn [, thisArg]);
Array.forEach()Performs the specified action for each element in an array.\t\n array1.forEach(callbackfn[, thisArg])
Array.from(arrayLike,mapfn,thisArg)Returns an array from an array-like or iterable object.\t\n Array.from (arrayLike [ , mapfn [ , thisArg ] ] );
Array.indexOf()Returns the index of the first occurrence of a value in an array.\t\n array1.indexOf(searchElement[, fromIndex])
Array.isArray(object)Determines whether an object is an array.\t\n Array.isArray(object) 
Array.join(arrayObj,separator)Adds all the elements of an array separated by the specified separator string.\t\n arrayObj.join([separator]) 
Array.keys(arrayObj)Returns an iterator that returns the index values of the array.\t\n arrayObj.keys();
Array.lastIndexOf()Returns the index of the last occurrence of a specified value in an array.\t\n array1.lastIndexOf(searchElement[, fromIndex])
Array.length(numVar,arrayObj)Gets or sets the length of the array. This is a number one higher than the highest element defined in an array.\t\nnumVar = arrayObj.length 
Array.map()Calls a defined callback function on each element of an array,\t\nand returns an array that contains the results.\t\n array1.map(callbackfn[, thisArg])
Array.of(element0,...,elementN)Returns an array from the passed in arguments.\t\n Array.of(element0[, element1][, ...][,elementN]);
Array.pop()Removes the last element from an array and returns it.\t\n arrayObj.pop()
Array.prototype()Returns a reference to the prototype for a class of array.\t\n array.prototype
Array.push(arrayObj,item,,item2,.,.,.,,itemN)Appends new elements to an array, and returns the new length of the array.\t\n arrayObj.push([item1 [item2 [. . . [itemN ]]]])
Array.reduce()Calls the specified callback function for all the elements in an array.\t\nThe return value of the callback function is the accumulated result,\t\nand is provided as an argument in the next call to the callback function.\t\narray1.reduce(callbackfn[, initialValue])
Array.reduceRight()Calls the specified callback function for all the elements in an array, in descending order.\t\nThe Callbacks return value is provided as an argument in the next call to the callback function.\t\n array1.reduceRight(callbackf
Array.reverse(arrayObj)Reverses the elements in an Array.\t\n arrayObj.reverse() 
Array.shift()Removes the first element from an array and returns it.\t\n arrayObj.shift()
Array.slice(arrayObj,start,,end,)Returns a section of an array.\t\n arrayObj.slice(start, [end]) 
Array.some()Determines whether the specified callback function returns\t\ntrue for any element of an array.\t\narray1.some(callbackfn[, thisArg])
Array.sort(arrayObj,sortFunction)Sorts an Array.\t\n arrayobj.sort(sortFunction) 
Array.splice(arrayObj,start,deleteCount,item1,,item2,.,.,.,,itemN)Removes elements from an array and, if necessary,\t\ninserts new elements in their place, returning the deleted elements.\t\n arrayObj.splice(start, deleteCount, [item1[, item2[, . . . [,itemN]]]])
Array.toString(array)Returns a string representation of an array.\t\n array.toString()
Array.unshift(arrayObj,item1,,item2,.,.,.,,itemN)Inserts new elements at the start of an array.\t\n arrayObj.unshift([item1[, item2 [, . . . [, itemN]]]])
Array.valueOf()Returns the primitive value of the specified object.\t\n array.valueOf()
Array.values(arrayObj)Returns an iterator that returns the values of the array.\t\n arrayObj.values();
ArrayBuffer.byteLength()Read-only. The length of the ArrayBuffer (in bytes).\t\n var arrayLength = arrayBuffer.byteLength
ArrayBuffer.isView(object)Determines whether an object provides a view of the buffer.\t\n ArrayBuffer.isView(object)
ArrayBuffer.slice(arrayBufferObj,start,,end,)Returns a section of an ArrayBuffer.\t\n arrayBufferObj.slice(start, [end]) 
Boolean.constructor(boolean,value)Specifies the function that creates a Boolean.\t\n boolean.constructor([[value])
Boolean.prototype()Returns a reference to the prototype for a Boolean.\t\n boolean.prototype
Boolean.toString(boolean)Returns a string representation of an object.\t\n boolean.toString()
Boolean.valueOf()Returns the primitive value of the specified boolean.\t\n boolean.valueOf()
DataView.buffer()Read-only. Gets the ArrayBuffer that is referenced by this view.\t\n var arrayBuffer = dataView.buffer
DataView.byteLength()Read-only. The length of this view from the start of its ArrayBuffer,in bytes,\t\nas fixed at construction time.\t\n var byteLength = dataView.byteLength
DataView.byteOffset()Read-only. The offset of this view from the start of its ArrayBuffer,in bytes,\t\nas fixed at construction time.\t\n var arrayOffset = dataView.byteOffset
DataView.getFloat32(testFloat,byteOffset,littleEndian)Gets the Float32 value at the specified byte offset from the start of the view.\t\n var testFloat = dataView.getFloat32(byteOffset, littleEndian); 
DataView.getFloat64(testFloat,byteOffset,littleEndian)Gets the Float64 value at the specified byte offset from the start of the view.\t\n var testFloat = dataView.getFloat64(byteOffset, littleEndian); 
DataView.getInt16(testInt,byteOffset,littleEndian)Gets the Int16 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getInt16(byteOffset, littleEndian); 
DataView.getInt32(testInt,byteOffset,littleEndian)Gets the Int32 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getInt32(byteOffset, littleEndian); 
DataView.getInt8(testInt,byteOffset)Gets the Int8 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getInt8(byteOffset); 
DataView.getUint16(testInt,byteOffset,littleEndian)Gets the Uint16 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getUint16(byteOffset, littleEndian); 
DataView.getUint32(testInt,byteOffset,littleEndian)Gets the Uint32 value at the specified byte offset from the start of the view.\t\n var req = new XMLHttpRequest();\t\n  req.open('GET', "http://www.example
DataView.getUint8(testInt,byteOffset)Gets the Uint8 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getUint8(byteOffset); 
DataView.setFloat32(byteOffset,value,littleEndian)Sets the Float32 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setFloat32 (byteOffset, value, littleEndian); 
DataView.setFloat64(byteOffset,value,littleEndian)Sets the Float64 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setFloat64 (byteOffset, value, littleEndian); 
DataView.setInt16(byteOffset,value,littleEndian)Sets the Int16 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setInt16(byteOffset, value, littleEndian); 
DataView.setInt32(byteOffset,value,littleEndian)Sets the Int32 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setInt32 (byteOffset, value, littleEndian); 
DataView.setInt8(byteOffset,value)Stores an Int8 value at the specified byte offset from the\t\nstart of the view.\t\n dataView.setInt8(byteOffset, value); 
DataView.setUint16(testInt,value,byteOffset,littleEndian)Sets the Uint16 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setUint16(byteOffset, value, littleEndian); 
DataView.setUint32(byteOffset,value,littleEndian)Sets the Uint32 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setUint32 (byteOffset, value, littleEndian); 
DataView.setUint8(byteOffset,value)Stores a Uint8 value at the specified byte offset from the\t\nstart of the view.\t\n dataView.setUint8(byteOffset, value); 
Date.constructor()Specifies the function that creates a date.\t\n date.constructor
Date.getDate()Gets the day-of-the-month, using local time.\t\n dateObj.getDate() 
Date.getDay()Gets the day of the week, using local time.\t\n dateObj. getDay() 
Date.getFullYear()Gets the year, using local time.\t\n dateObj.getFullYear() 
Date.getHours()Gets the hours in a date, using local time.\t\n dateObj.getHours() 
Date.getMilliseconds()Gets the milliseconds of a Date, using local time.\t\n dateObj.getMilliseconds() 
Date.getMinutes()Gets the minutes of a Date object, using local time.\t\n dateObj.getMinutes() 
Date.getMonth()Gets the month, using local time.\t\n dateObj.getMonth() 
Date.getSeconds()Gets the seconds of a Date object, using local time.\t\n dateObj.getSeconds() 
Date.getTime()Gets the time value in milliseconds.\t\n dateObj.getTime() 
Date.getTimezoneOffset()Gets the difference in minutes between the time on the local\t\ncomputer and Universal Coordinated Time (UTC).\t\n dateObj.getTimezoneOffset() 
Date.getUTCDate()Gets the day-of-the-month, using Universal Coordinated Time (UTC).\t\n dateObj.getUTCDate() 
Date.getUTCDay()Gets the day of the week using Universal Coordinated Time (UTC).\t\n dateObj.getUTCDay() 
Date.getUTCFullYear()Gets the year using Universal Coordinated Time (UTC).\t\n dateObj.getUTCFullYear() 
Date.getUTCHours()Gets the hours value in a Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.getUTCHours() 
Date.getUTCMilliseconds()Gets the milliseconds of a Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.getUTCMilliseconds() 
Date.getUTCMinutes()Gets the minutes of a Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.getUTCMinutes() 
Date.getUTCMonth()Gets the month of a Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.getUTCMonth() 
Date.getUTCSeconds()Gets the seconds of a Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.getUTCSeconds() 
Date.getVarDate()Returns a VT_DATE value from a Date object.\t\ndateObj.getVarDate() 
Date.getYear()Gets the year of a Date object.\t\ndateObj.getYear() 
Date.now()Gets the current date and time.\t\n Date.now()
Date.parse()Parses a string containing a date, and returns the number\t\nof milliseconds between that date and midnight, January 1, 1970.\t\n Date.parse(dateVal) 
Date.prototype()Returns a reference to the prototype for a date.\t\n date.prototype
Date.setDate(dateObj,numDate)Sets the numeric day-of-the-month value of the Date object\t\nusing local time.\t\n dateObj.setDate(numDate) 
Date.setFullYear(dateObj,,numYear,numMonth,numDate)Sets the year of the Date object using local time.\t\n dateObj.setFullYear(numYear[, numMonth[, numDate]]) 
Date.setHours(dateObj,numHours,numMin,numSec,numMilli)Sets the hour value in the Date object using local time.\t\n dateObj.setHours(numHours[, numMin[, numSec[, numMilli]]]) 
Date.setMilliseconds(dateObj,numMilli)Sets the milliseconds value in the Date object using local time.\t\n dateObj.setMilliseconds(numMilli) 
Date.setMinutes(dateObj,numMinutes,numSeconds,numMilli)Sets the minutes value in the Date object using local time.\t\n dateObj.setMinutes(numMinutes[, numSeconds[, numMilli]]) 
Date.setMonth(dateObj,numMonth,dateVal)Sets the month value in the Date object using local time.\t\n dateObj. setMonth(numMonth[, dateVal]) 
Date.setSeconds(dateObj,numSeconds,numMilli)Sets the seconds value in the Date object using local time.\t\n dateObj.setSeconds(numSeconds[, numMilli]) 
Date.setTime(dateObj,milliseconds)Sets the date and time value in the Date object.\t\n dateObj.setTime(milliseconds) 
Date.setUTCDate(dateObj,numDate)Sets the numeric day of the month in the Date object using\t\nUniversal Coordinated Time (UTC).\t\n dateObj.setUTCDate(numDate) 
Date.setUTCFullYear(dateObj,numYear,numMonth,numDate)Sets the year value in the Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.setUTCFullYear(numYear[, numMonth[, numDate]]) 
Date.setUTCHours(dateObj,numHours,numMin,numSec,numMilli)Sets the hours value in the Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.setUTCHours(numHours[, numMin[, numSec[, numMilli]]]) 
Date.setUTCMilliseconds(dateObj,numMilli)Sets the milliseconds value in the Date object using Universal\t\nCoordinated Time (UTC).\t\n dateObj.setUTCMilliseconds(numMilli) 
Date.setUTCMinutes(dateObj,numMinutes,numSeconds,numMilli)Sets the minutes value in the Date object using Universal\t\nCoordinated Time (UTC).\t\n dateObj.setUTCMinutes(numMinutes[, numSeconds[, numMilli]]) 
Date.setUTCMonth(dateObj,numMonth,dateVal)Sets the month value in the Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.setUTCMonth(numMonth[, dateVal]) 
Date.setUTCSeconds(dateObj,numSeconds,numMilli)Sets the seconds value in the Date object using Universal\t\nCoordinated Time (UTC).\t\n dateObj.setUTCSeconds(numSeconds[, numMilli]) 
Date.setYear(dateObj,numYear)Sets the year value in the Date object.\t\ndateObj.setYear(numYear) 
Date.toDateString()Returns a date as a string value.\t\n objDate.toDateString()
Date.toGMTString()Returns a date converted to a string using Greenwich Mean Time(GMT).\t\n dateObj.toGMTString() 
Date.toISOString()Returns a date as a string value in ISO format.\t\n objDate.toISOString()
Date.toJSON(objectname)Used by the JSON.stringify method to enable the transformation\t\nof an object's data for JavaScript Object Notation (JSON) serialization.\t\n objectname.toJSON()
Date.toLocaleDateString(dateObj,locales,options)Returns a date as a string value that is appropriate to the\t\nhost environment's current locale or the specified locale.\t\ndateObj.toLocaleDateString( [locales][, options]) 
Date.toLocaleString(dateObj,locales,options)Converts a date to a string by using the current or specified locale.\t\ndateObj.toLocaleString([locales][, options]) 
Date.toLocaleTimeString(dateObj,locales,options)Returns a time as a string value that is appropriate to the\t\nhost environment's current locale or a specified locale.\t\ndateObj.toLocaleTimeString([locales][, options])
Date.toString(date)Returns a string representation of a date.\t\nThe format of the string depends on the locale. For U.S. English (en-us), it is as follows:\t\n date.toString()
Date.toTimeString()Returns a time as a string value.\t\n objDate. toTimeString()
Date.toUTCString()Returns a date converted to a string using Universal Coordinated\t\nTime (UTC).\t\n dateObj.toUTCString() 
Date.UTC(year,month,day,hours,minutes,seconds,ms)Returns the number of milliseconds between midnight, January\t\n1, 1970 Universal Coordinated Time (UTC) (or GMT) and the specified date.\t\n Date.UTC(year, month, day[, hours[, minutes[, seconds[,ms]]]]) 
Date.valueOf()Returns the stored time value in milliseconds since midnight,\t\nJanuary 1, 1970 UTC.\t\n date.valueOf()
Debug.debuggerEnabled()Determines whether debugging is enabled for the script context.\t\nDebugging may be enabled or disabled whether or not a debugger is attached.\t\n var dbgEnabled = Debug.debuggerEnabled
Debug.msTraceAsyncCallbackCompleted()Indicates that the callback stack associated with a previously\t\nspecified asynchronous operation has completed.\t\nDebug.msTraceAsyncCallbackCompleted()
Debug.msTraceAsyncCallbackStarting(asyncOperationId)Associates the callback stack with a previously specified asynchronous operation.\t\nDebug.msTraceAsyncCallbackStarting(asyncOperationId)
Debug.msTraceAsyncOperationCompleted(asyncOperationId,status)Indicates that an asynchronous operation has completed.\t\nDebug.msTraceAsyncOperationCompleted(asyncOperationId, status)
Debug.msTraceAsyncOperationStarting(operationName)Initiates a trace for an asynchronous operation.\t\nDebug.msTraceAsyncOperationStarting(operationName)
Debug.msUpdateAsyncCallbackRelation(relatedAsyncOperationId,relationType)Updates the relationship status between a synchronous work\t\nitem and the associated asynchronous operation.\t\nDebug.msUpdateAsyncCallbackRelation(relatedAsyncOperationId, relationType)
Debug.setNonUserCodeExceptions()Determines whether any try-catch blocks in this scope are to be treated by the debugger as user-unhandled.\t\nExceptions can be classified as thrown, user-unhandled or unhandled.\t\n Debug.setNonUserCodeExceptions [= bool]
Debug.write(str1, str2, ... , strN)Sends strings to the script debugger.\t\nDebug.write([str1 [, str2 [, ... [, strN]]]])
Debug.writeln(str1, str2, ... , strN)Sends strings to the script debugger, followed by a newline\t\ncharacter.\t\nDebug.writeln([str1 [, str2 [, ... [, strN]]]])
Enumerator.atEnd()Returns a Boolean value indicating if the enumerator is at\t\nthe end of the collection.\t\nmyEnum.atEnd()
Enumerator.item()Returns the current item in the collection.\t\nenumObj.item() 
Enumerator.moveFirst()Resets the current item in the collection to the first item.\t\nenumObj.moveFirst( ) 
Enumerator.moveNext()Moves the current item to the next item in the collection.\t\nenumObj.moveNext( ) 
Error.constructor()Specifies the function that creates an Error.\t\n error.constructor
Error.description(object,stringExpression)Returns or sets the descriptive string associated with a specific\t\nerror.\t\n object.description [= stringExpression]
Error.message(errorObj)Returns an error message string.\t\n errorObj.message
Error.name(errorObj)Returns the name of an error.\t\n errorObj.name
Error.number(Object,errorNumber)Returns or sets the numeric value associated with a specific error.\t\nThe Error object's default property is number.\t\n object.number [= errorNumber]
Error.prototype()Returns a reference to the prototype for an error.\t\n error.prototype
Error.stack()Gets or sets the error stack as a string that contains the\t\nstack trace frames.\t\n object.stack 
Error.stackTraceLimit()Gets or sets the stack trace limit, which is equivalent to the number of error frames to display.\t\nThe default limit is 10.\t\n Error.stackTraceLimit 
Error.toString(date)Returns a string representation of an error.\t\n error.toString()
Error.valueOf()Returns the string value of an error.\t\n error.valueOf()
Float32Array.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = float32Array.buffer
Float32Array.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = float32Array.byteLength
Float32Array.byteOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = float32Array.byteOffset
Float32Array.BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = int8Array.BYTES_PER_ELEMENT
Float32Array.get(value,index)Omittable. Gets the element at the specified index.\t\n var value = float32Array.get(index);
Float32Array.length()The length of the array.\t\n var arrayLength = float32Array.length
Float32Array.set(index,value,array,offset)Sets a value or an array of values.\t\n float32Array.set(index, value);\t\nfloat32Array.set(array, offset);
Float32Array.subarray(newFloat32Array,begin,end)Gets a new Float32Array view of the ArrayBuffer Object store\t\nfor this array, specifying the first and last members of the subarray.\t\n var newFloat32Array = float32Array.subarray(begin, end);
Float64Array.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = float64Array.buffer
Float64Array.bufferOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.
Float64Array.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = float64Array.byteLength
Float64Array.BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = int8Array.BYTES_PER_ELEMENT
Float64Array.get(value,index)Omittable. Gets the element at the specified index.\t\n var value = float64Array.get(index);
Float64Array.length()The length of the array.\t\n var arrayLength = float64Array.length
Float64Array.set(index,value,array,offset)Sets a value or an array of values.\t\n float64Array.set(index, value);\t\nfloat64Array.set(array, offset);
Float64Array.subarray(newFloat64Array,begin,end)Gets a new Float64Array view of the ArrayBuffer Object store\t\nfor this array, specifying the first and last members of the subarray.\t\n var newFloat64Array = float64Array.subarray(begin, end);
Function.apply(thisObj,argArray)Calls the function, substituting the specified object for\t\nthe this value of the function, and the specified array for the arguments of the function.\t\n apply([thisObj[,argArray]])
Function.arguments()Gets the arguments for the currently executing Function object.\t\n function.arguments
Function.bind(function,thisArg,arg1[,arg2[,argN]]])For a given function, creates a bound function that has the same body as the original function.\t\nIn the bound function, the this object resolves to the passed in object.\t\nThe bound function has the specified initial parameters.\t\n functi
Function.call(thisObj,arg1,,arg2,,,,argN)Calls a method of an object, substituting another object for\t\nthe current object.\t\n call([thisObj[, arg1[, arg2[,  [, argN]]]]])
Function.caller()Gets the function that invoked the current function.\t\n functionName.caller
Function.length()Gets the number of arguments defined for a function.\t\n functionName.length
Global.decodeURI()Gets the unencoded version of an encoded Uniform Resource\t\nIdentifier (URI).\t\n decodeURI(URIstring)
Global.decodeURIComponent()Gets the unencoded version of an encoded component of a Uniform\t\nResource Identifier (URI).\t\n decodeURIComponent(encodedURIString)
Global.encodeURI()Encodes a text string as a valid Uniform Resource Identifier (URI)----\t\nencodeURI(URIString)
Global.encodeURIComponent()Encodes a text string as a valid component of a Uniform Resource\t\nIdentifier (URI).\t\n encodeURIComponent(encodedURIString)
Global.escape()Encodes strings so they can be read on all computers.\t\nDeprecated.\t\nescape(charString) 
Global.eval(codeString )Evaluates JavaScript code and executes it.\t\n eval(codeString) 
Global.GetObject(pathname,class,appname,objectype)Returns a reference to an Automation object from a file.\t\nGetObject([pathname] [, class])
Global.Infinity()A number that is larger than the largest floating point number.\t\nReturns an initial value of Number.POSITIVE_INFINITY. Negative Infinity (-Infinity) is smaller than the smallest floating point number.\t\n Infinity 
Global.isFinite()Determines whether a supplied number is finite.\t\n isFinite(number) 
Global.isNaN()Returns a Boolean value that indicates whether a value is\t\nthe reserved value NaN (not a number).\t\n isNaN(numValue) 
Global.NaN()A special constant that specifies thatNot an expression is\t\nnot a number.\t\n NaN 
Global.null()
Global.parseFloat()Converts a string to a floating-point number.\t\n parseFloat(numString) 
Global.parseInt(numString,radix)Converts a string to an integer.\t\nparseInt(numString, [radix]) 
Global.ScriptEngine()Gets the name of the scripting language in use.\t\n ScriptEngine()
Global.ScriptEngineBuildVersion()Gets the build version number of the scripting engine in use.\t\n ScriptEngineBuildVersion()
Global.ScriptEngineMajorVersion()Gets the major version number of the scripting engine in use.\t\n ScriptEngineMajorVersion()
Global.ScriptEngineMinorVersion()Gets the minor version number of the scripting engine in use.\t\n ScriptEngineMinorVersion()
Global.undefined()A value that has never been defined, such as a variable that\t\nhas not been initialized.\t\n undefined
Global.unescape()Decodes String objects encoded with the escape function.\t\nDeprecated.\t\nunescape(charString) 
Int16Array.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = int16Array.buffer
Int16Array.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = int16Array.byteLength
Int16Array.byteOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = int16Array.byteOffset
Int16Array.BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = int8Array.BYTES_PER_ELEMENT
Int16Array.length()The length of the array.\t\n var arrayLength = int16Array.length
Int16Array.set(index,value,array,offset)Sets a value or an array of values.\t\n int16Array.set(index, value);\t\nint16Array.set(array, offset);
Int16Array.subarray(newInt16Array,begin,end)Gets a new Int16Array view of the ArrayBuffer Object store\t\nfor this array, specifying the first and last members of the subarray.\t\n var newInt16Array = int16Array.subarray(begin, end);
Int32Array.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = int32Array.buffer
Int32Array.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = int32Array.byteLength
Int32Array.byteOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = int32Array.byteOffset
Int32Array.BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = int32Array.BYTES_PER_ELEMENT
Int32Array.length()The length of the array.\t\n var arrayLength = int32Array.length
Int32Array.set(index,value,array,offset)Sets a value or an array of values.\t\n Int32Array.set(index, value);\t\nint32Array.set(array, offset);
Int32Array.subarray(newInt32Array,begin,end)Gets a new Int32Array view of the ArrayBuffer Object store\t\nfor this array, specifying the first and last members of the subarray.\t\n var newInt32Array = int32Array.subarray(begin, end);
Int8Array.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = int8Array.buffer
Int8Array.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = int8Array.byteLength
Int8Array.byteOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = int8Array.byteOffset
Int8Array.BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = int8Array.BYTES_PER_ELEMENT
Int8Array.length()The length of the array.\t\n var arrayLength = int8Array.length
Int8Array.set(index,value,array,offset)Sets a value or an array of values.\t\n int8Array.set(index, value);\t\nint8Array.set(array, offset);
Int8Array.subarray(newInt8Array,begin,end)Gets a new Int8Array view of the ArrayBuffer store for this\t\narray, referencing the elements at begin, inclusive, up to end, exclusive.\t\n var req = new XMLHttpRequest();\t\n  req.open('GET', "http://www.example.com");\t\n  req.responseType = "arra
Intl.Collator.compare(collatorObj)Returns a function that compares two strings by using the\t\ncollator’s sort order.\t\n collatorObj.compare
Intl.Collator.constructor()Specifies the function that creates a collator.\t\n collator.constructor
Intl.Collator.prototype()Returns a reference to the prototype for a collator.\t\n collator.prototype
Intl.Collator.resolvedOptions(collatorObj)Returns an object containing the properties and values of\t\nthe collator object.\t\n collatorObj.resolvedOptions()
Intl.Collator(collatorObj,locales,options)Provides locale-specific string comparisons.\t\n collatorObj = new Intl.Collator([locales][, options])
Intl.DateTimeFormat.constructor()Specifies the function that creates a formatter.\t\n dateTimeFormatter.constructor
Intl.DateTimeFormat.format(dateTimeFormatObj)Returns a function that formats a locale-specific date by\t\nusing the specified date/time formatter settings.\t\n dateTimeFormatObj.format
Intl.DateTimeFormat.prototype()Returns a reference to the prototype for a formatter.\t\n dateTimeFormat.prototype
Intl.DateTimeFormat.resolvedOptions(dateTimeFormatObj)Returns an object that contains the properties and values\t\nof the date/time formatter object.\t\n dateTimeFormatObj.resolvedOptions()
Intl.DateTimeFormat(dateTimeFormatObj,locales,options)Provides locale-specific date and time formatting.\t\n dateTimeFormatObj = new Intl.DateTimeFormat([locales][, options])
Intl.NumberFormat.constructor()Specifies the function that creates a formatter.\t\n numberFormatter.constructor
Intl.NumberFormat.format(numberFormatObj)Returns a function that formats a locale-specific number by\t\nusing the specified number formatter settings.\t\n numberFormatObj.format
Intl.NumberFormat.prototype()Returns a reference to the prototype for a formatter.\t\n numberFormat.prototype
Intl.NumberFormat.resolvedOptions(numberFormatObj)Returns an object that contains the properties and values\t\nof the NumberFormat formatter object.\t\n numberFormatObj.resolvedOptions()
Intl.NumberFormat(numberFormatObj,locales,options)Provides locale-specific number formatting.\t\n numberFormatObj = new Intl.NumberFormat([locales][, options])
JSON.parse(text,reviver)Converts a JavaScript Object Notation (JSON) string into an object.\t\n JSON.parse(text [, reviver])
JSON.stringify(value,replacer,space)Converts a JavaScript value to a JavaScript Object Notation\t\n(JSON) string.\t\n JSON.stringify(value [, replacer] [, space])
Map.clear(mapObj)Removes all elements from a map.\t\n mapObj.clear()
Map.constructor()Specifies the function that creates a map.\t\n map.constructor
Map.delete(mapObj,key)Removes the specified element from a map.\t\n mapObj.delete(key)
Map.forEach(mapObj,callbackfn,thisArg)Performs the specified action for each element in a map.\t\n mapObj.forEach(callbackfn[, thisArg])
Map.get(mapObj,key)Returns a specified element from a map.\t\n mapObj.get(key)
Map.has(mapObj,key)Returns true if the map contains the specified element.\t\n mapObj.has(key)
Map.prototype()Returns a reference to the prototype for a map.\t\n map.prototype
Map.set(mapObj,key,value)Adds a new element to a map.\t\n mapObj.set(key, value)
Map.size(sizeVar,mapObj)Returns the number of elements in a map.\t\n sizeVar = mapObj.size
Map.toString(mapObj)Returns a string representation of a map.\t\n mapObj.toString()
Map.valueOf(mapObj)Returns the primitive value of the specified object.\t\n mapObj.valueOf()
Math.abs()Returns the absolute value of a number (the value without regard to whether it is positive or negative).\t\nFor example, the absolute value of -5 is the same as the absolute value of 5.\t\n Math.abs(number)
Math.acos()Returns the arc cosine (or inverse cosine) of a number.\t\n Math.acos(number)
Math.acosh()Returns the hyperbolic arccosine (or inverse hyperbolic cosine)of a number.\t\n Math.acosh(number)
Math.asin()Returns the arcsine of a number.\t\n Math.asin(number) 
Math.asinh()Returns the inverse hyperbolic sine of a number.\t\n Math.asinh(x) 
Math.atan()Returns the arctangent of a number.\t\n Math.atan(number) 
Math.atan2(x,y)Returns the angle (in radians) from the X axis to a point (y,x).\t\n Math.atan2(y, x)
Math.atanh()Returns the inverse hyperbolic tangent of a number.\t\n Math.atanh(x) 
Math.cbrt()Returns the cube root of a number.\t\n Math.cbrt(number) 
Math.ceil()Returns the smallest integer greater than or equal to its\t\nnumeric argument.\t\n Math.ceil(number) 
Math.clz32()Returns the number of leading zero bits in the 32-bit binary\t\nrepresentation of a number.\t\n Math.clz32(number) 
Math.cos()Returns the cosine of a number.\t\n Math.cos(radians) 
Math.cosh()Returns the hyperbolic cosine of a number.\t\n Math.cosh(x) 
Math.exp()Returns e (the base of natural logarithms) raised to a power.\t\n Math.exp(number) 
Math.expm1()Returns the result of subtracting 1 from e raised to a power).\t\n Math.expm1(number) 
Math.floor()Returns the greatest integer less than or equal to its numeric argument.\t\n Math.floor(number) 
Math.fround(Returns the nearest single-precision floating-point format\t\nof a number.\t\n Math.fround(number) 
Math.hypot(value1,value2,values)Returns the square root of the sum of the squares of the arguments.\t\n Math.hypot ( value1[, value2[, ...values] );
Math.imul(x,y)Returns the product of two numbers that are treated as 32-bit\t\nsigned integers.\t\n Math.imul(x, y);
Math.log(number)Returns the natural logarithm (base e) of a number.\t\n Math.log(number) 
Math.log10()Returns the base 10 logarithm of a number.\t\n Math.log10(x) 
Math.log1p()Returns the natural logarithm of 1 + a number.\t\n Math.log1p(x) 
Math.log2()Returns the base 2 logarithm of a number.\t\n Math.log2(x) 
Math.max()Returns the larger of a set of supplied numeric expressions.\t\n Math.max([number1[, number2[... [, numberN]]]]) 
Math.min()Returns the smaller of a set of numeric expressions.\t\n Math.min([number1[, number2[... [,numberN]]]])
Math.pow(base,exponent)Returns the value of a base expression taken to a specified power.\t\n Math.pow(base, exponent) 
Math.random()Returns a pseudorandom number between 0 and 1.\t\n Math.random( ) 
Math.round()Returns a supplied numeric expression rounded to the nearest\t\ninteger.\t\n Math.round(number) 
Math.sign()Returns the sign of a number, indicating whether the number\t\nis positive, negative, or 0.\t\n Math.sign(number)
Math.sin()Returns the sine of a number.\t\n Math.sin(radians) 
Math.sinh()Returns the inverse hyperbolic sine of a number.\t\n Math.sinh(x) 
Math.sqrt()Returns the square root of a number.\t\n Math.sqrt(number) 
Math.tan()Returns the tangent of a number.\t\n Math.tan(radians) 
Math.tanh()Returns the hyperbolic tangent of a number.\t\n Math.tanh(x)
Math.trunc()Returns the integer portion of a number, removing any fractional\t\ndigits.\t\n Math.trunc(number)
Number.constructor()Specifies the function that creates a Number.\t\n number.constructor
Number.isFinite()Returns a Boolean value that indicates whether a value is\t\na finite number.\t\n Number.isFinite(numValue)
Number.isInteger()Returns a Boolean value that indicates whether a value is\t\nan integer.\t\n Number.isInteger(numValue)
Number.isNaN(numValue) Returns a Boolean value that indicates whether a value is\t\nthe reserved value NaN (not a number).\t\n Number.isNaN(numValue)
Number.isSafeInteger()Returns a Boolean value that indicates whether a number can\t\nbe safely represented in JavaScript.\t\n Number.isSafeInteger(numValue) 
Number.prototype()Returns a reference to the prototype for a class of number.\t\n number.prototype
Number.toExponential(numObj,fractionDigits)Represents a number in exponential notation.\t\n numObj. toExponential([fractionDigits])
Number.toFixed(numObj,fractionDigits)Represents a number in fixed-point notation.\t\n numObj.toFixed([fractionDigits])
Number.toLocaleString(numberObj,locales,options)Converts a number to a string by using the current or specified locale.\t\nnumberObj.toLocaleString([locales][, options]) 
Number.toPrecision(numObj,precision)Represents a number either in exponential or fixed-point notation\t\nwith a specified number of digits.\t\n numObj.toPrecision([precision])
Number.toString(number)Returns a string representation of a number.\t\n number.toString()
Number.valueOf()Returns the primitive value of the specified number.\t\n number.valueOf()
Object.__proto__(object)Contains a reference to the internal prototype of the specified\t\nobject.\t\nobject.__proto__
Object.assign(target,...sources)Copies the values from one or more source objects to a target\t\nobject.\t\n Object.assign(target, ...sources );
Object.constructor()Specifies the function that creates an object.\t\n object.constructor
Object.create(prototype,descriptors)Creates an object that has the specified prototype, and that\t\noptionally contains specified properties.\t\n Object.create(prototype, descriptors)
Object.defineProperties(object,descriptors)Adds one or more properties to an object, and/or modifies\t\nattributes of existing properties.\t\n var newLine = "<br />",\t\n\t\nvar obj = {},\t\nObject.defineProperties(obj, {\t\n  newDataProperty: {\t\n      value: 101,\t\n      writable: true,\t
Object.defineProperty(object,propertyname,descriptor)Adds a property to an object, or modifies attributes of an\t\nexisting property.\t\n Object.defineProperty(object, propertyname, descriptor)
Object.freeze(object)Prevents the modification of existing property attributes\t\nand values, and prevents the addition of new properties.\t\n Object.freeze(object)
Object.getOwnPropertyDescriptor(object,propertyname)Gets the own property descriptor of the specified object.\t\nAn own property descriptor is one that is defined directly on the object and is not inherited from the object's prototype.\t\n Object.getOwnPropertyDescriptor(object, propertyname)
Object.getOwnPropertyNames()Returns the names of the own properties of an object.\t\nThe own properties of an object are those that are defined directly on that object, and are not inherited from the object's prototype. The properties of an object include both fields (objects) and functions.\t\n----
Object.getOwnPropertySymbols(object)Returns the own symbol properties of an object.\t\nThe own symbol properties of an object are those that are defined directly on that object, and are not inherited from the object's prototype.\t\n Object.getOwnPropertySymbols(object);
Object.getPrototypeOf(object)Returns the prototype of an object.\t\n Object.getPrototypeOf(object)
Object.hasOwnProperty(object,proName)Determines whether an object has a property with the specified name.\t\n object.hasOwnProperty(proName)
Object.is(value1,value2)Returns a value that indicates whether two values are the\t\nsame value.\t\n Object.is(value1, value2)
Object.isExtensible(object)Returns a value that indicates whether new properties can\t\nbe added to an object.\t\n Object.isExtensible(object)
Object.isFrozen(object)Returns true if existing property attributes and values cannot\t\nbe modified in an object, and new properties cannot be added to the object.\t\n Object.isFrozen(object)
Object.isPrototypeOf(prototype,object)Determines whether an object exists in another object's prototype\t\nchain.\t\n prototype.isPrototypeOf(object)
Object.isSealed(object)Returns true if existing property attributes cannot be modified\t\nin an object and new properties cannot be added to the object.\t\n Object.isSealed(object)
Object.keys()Returns the names of the enumerable properties and methods\t\nof an object.\t\n Object.keys(object)
Object.preventExtensions(object)Prevents the addition of new properties to an object.\t\n Object.preventExtensions(object)
Object.propertyIsEnumerable(object,proName)Determines whether a specified property is enumerable.\t\n object. propertyIsEnumerable(proName)
Object.prototype()Returns a reference to the prototype for a class of objects.\t\n objectName.prototype
Object.seal(object)Prevents the modification of attributes of existing properties,\t\nand prevents the addition of new properties.\t\n Object.seal(object)
Object.setPrototypeOf(obj,proto)Sets the prototype of an object.\t\nObject.setPrototypeOf(obj, proto);
Object.toLocaleString()Returns a date converted to a string using the current locale.\t\ndateObj.toLocaleString() 
Object.toString(objectname,radix)Returns a string representation of an object.\t\n objectname.toString([radix])
Object.valueOf()Returns the primitive value of the specified object.\t\n object.valueOf()
Promise.all (func1,func2,funcN)Joins two or more promises and returns only when all the specified\t\npromises have completed or been rejected.\t\n Promise.all(func1, func2 [,funcN])
Promise.catch(promise,onRejected)Allows you to specify work to be done on the rejection of a promise.\t\n promise.catch(onRejected)
Promise.race(iterable)Creates a new promise that will resolve or reject with the\t\nsame result value as the first promise to resolve or reject among the passed in arguments.\t\n Promise.race(iterable)
Promise.reject(r)Creates a new rejected promise with a result equal to the\t\npassed in argument.\t\n Promise.reject(r);
Promise.resolve(x)Creates a new resolved promise with a result equal to its argument.\t\n Promise.resolve(x)
Promise.then(promise,onCompleted,onRejected)Allows you to specify work to be done on the fulfillment of\t\na promise.\t\n promise.then(onCompleted, onRejected);
RegExp.{text}()Returns the characters from the position following the last match to the end of the searched string.\t\nRead-only.
RegExp.index()Returns the character position where the first successful match begins in a searched string.-Read-only.\t\n RegExp.index 
RegExp.input()Returns the string against which a regular expression search was performed.-Read-only.\t\n RegExp.input
RegExp.lastIndex()Returns the character position where the next match begins\t\nin a searched string.\t\n RegExp.lastIndex
RegExp.lastMatch()Returns the last matched characters from any regular expression search.-Read-only.\t\n RegExp.lastMatch
RegExp.lastParen()Returns the last parenthesized submatch from any regular expression search, if any.-Read-only.\t\n RegExp.lastParen
RegExp.leftContext()Returns the characters from the beginning of a searched string up to the position before the beginning of the last match.-Read-only.\t\n RegExp.leftContext
Regular.compile(rgExp,pattern,flags)Compiles a regular expression into an internal format for\t\nfaster execution.\t\n rgExp.compile(pattern, [flags]) 
Regular.exec(rgExp,str)Executes a search on a string using a regular expression pattern,\t\nand returns an array containing the results of that search.\t\n rgExp.exec(str) 
Regular.global()Returns a Boolean value indicating the state of the global flag (g) used with a regular expression.\t\nDefault is false. Read-only.\t\n rgExp.global
Regular.ignoreCase()Returns a Boolean value indicating the state of the ignoreCase flag (i) used with a regular expression.\t\nDefault is false. Read-only.\t\n rgExp.ignoreCase
Regular.multiline()Returns a Boolean value indicating the state of the multiline flag (m) used with a regular expression.\t\nDefault is false. Read-only.\t\n rgExp.multiline
Regular.source()Returns a copy of the text of the regular expression pattern.\t\nRead-only. The rgExp argument is a Regular expression object. It can be a variable name or a literal.\t\n rgExp.source
Regular.test(rgExp,str)Returns a Boolean value that indicates whether or not a pattern\t\nexists in a searched string.\t\n rgExp.test(str) 
Set.add(setObj,value)Adds a new element to a set.\t\n setObj.add(value)
Set.clear(setObj)Removes all elements from the set.\t\n setObj.clear()
Set.constructor()Specifies the function that creates a set.\t\n set.constructor
Set.delete(setObj,value)Removes the specified element from a set.\t\n setObj.delete(value)
Set.forEach(setObj,callbackfn,thisArg)Performs the specified action for each element in a set.\t\n setObj.forEach(callbackfn[, thisArg])
Set.has(setObj,value)Returns true if the set contains the specified element.\t\n setObj.has(value)
Set.prototype()Returns a reference to the prototype for a set.\t\n set.prototype
Set.size(sizeVar,setObj)Returns the number of elements in a set.\t\n sizeVar = setObj.size
Set.toString(setObj)Returns a string representation of a set.\t\n setObj.toString()
Set.valueOf(setObj)Returns the primitive value of the specified object.\t\n setObj.valueOf()
String.charAt(strObj,index)Returns the character at the specified index.\t\n strObj. charAt(index)
String.charCodeAt(strObj,index)Returns the Unicode value of the character at the specified\t\nlocation.\t\n strObj. charCodeAt(index)
String.codePointAt(stringObj,pos)Returns the code point for a Unicode UTF-16 character.\t\n stringObj.codePointAt(pos);
String.concat(string1,,string2,.,.,.,,stringN)Returns a string that contains the concatenation of two or\t\nmore strings.\t\n string1. concat([string2[, string3[, . . . [, stringN]]]])
String.constructor()Specifies the function that creates a string.\t\n string.constructor
String.endsWith(stringObj,str,position)Returns a value that indicates whether a string or substring\t\nends with another specified string.\t\n stringObj.endsWith(str, [, position]);
String.fromCharCode(String,code1,,.,.,.,,,codeN)Returns a string from a number of Unicode character values.\t\n String.fromCharCode([code1[, code2[, ...[, codeN]]]]) 
String.fromCodePoint(…codePoints)Returns the string associated with a Unicode UTF-16 code point.\t\n String.fromCodePoint(...codePoints);
String.HTML()You can use HTML tag methods to place HTML elements around\t\ntext in a String object.
String.includes(stringObj,substring,position)Returns a Boolean value that indicates whether the passed\t\nin string is contained in the string object.\t\n stringObj.includes(substring [, position]);
String.indexOf(strObj,subString,startIndex)Returns the position of the first occurrence of a substring.\t\n strObj. indexOf(subString[, startIndex])
String.lastIndexOf(strObj,substring,startindex)Returns the last occurrence of a substring in the string.\t\n strObj.lastIndexOf(substring[, startindex])
String.length()Returns the length of a String object.\t\nstrVariable.length\t\n"String Literal".length 
String.localeCompare(stringVar,stringExp,locales,options)Determines whether two strings are equivalent in the current\t\nlocale.\t\nstringVar.localeCompare(stringExp[, locales][, options]) 
String.match(stringObj,rgExp)Matches a string with a regular expression, and returns an\t\narray containing the results of that search.\t\n stringObj.match(rgExp) 
String.normalize(stringObj,form)Returns the Unicode Normalization Form of a specified string.\t\n stringObj.normalize([form]);
String.prototype()Returns a reference to the prototype for a class of string.\t\n string.prototype
String.raw(templateStr,obj,...substitutions)Returns the raw string form of a template string.\t\n String.raw`templateStr`,\t\nString.raw(obj, ...substitutions);
String.repeat(stringObj,count)Returns a new string object with a value equal to the original\t\nstring repeated the specified number of times.\t\n stringObj.repeat(count);
String.replace(stringObj,,rgExp,,replaceText,)Replaces text in a string, using a regular expression or search\t\nstring.\t\n stringObj. replace(rgExp, replaceText)
String.search(stringObj,rgExp)Finds the first substring match in a regular expression search.\t\n stringObj.search(rgExp) 
String.slice(stringObj,start,,end,)Returns a section of a string.\t\n stringObj.slice(start, [end]) 
String.split(stringObj,,separator,,limit)Split a string into substrings using the specified separator\t\nand return them as an array.\t\n stringObj.split([separator[, limit]])
String.startsWith(stringObj,str,position)Returns a value that indicates whether a string or substring\t\nstarts with another specified string.\t\n stringObj.startsWith(str, [, position]);
String.substr(stringvar,start,length)Gets a substring beginning at the specified location and having\t\nthe specified length.\t\n stringvar.substr(start [, length ]) 
String.substring(start,end)Returns the substring at the specified location within a String\t\nobject.\t\n"String Literal".substring(start [, end]) 
String.toLocaleLowerCase()Converts all alphabetic characters to lowercase, taking into\t\naccount the host environment's current locale.\t\n stringVar.toLocaleLowerCase()
String.toLocaleUpperCase()Returns a string where all alphabetic characters have been\t\nconverted to uppercase\t\n stringVar.toLocaleUpperCase()
String.toLowerCase()Converts all the alphabetic characters in a string to lowercase.\t\n"String Literal".toLowerCase() 
String.toString(string)Returns a string representation of a string.\t\n string.toString()
String.toUpperCase()Converts all the alphabetic characters in a string to uppercase.\t\n"String Literal".toUpperCase() 
String.trim(stringObj )Removes the leading and trailing white space and line terminator\t\ncharacters from a string.\t\n stringObj.trim()
String.valueOf()Returns the string.\t\n string.valueOf()
Symbol.for(key)Returns the symbol for a specified key or, if the key is not\t\npresent, creates a new symbol object with the new key.\t\n Symbol.for(key);
Symbol.keyFor(sym)Returns the key for a specified symbol.\t\n Symbol.keyFor(sym);
Uint16Array.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = uint16Array.buffer
Uint16Array.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = uint16Array.byteLength
Uint16Array.byteOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = uint16Array.byteOffset
Uint16Array.BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = uint16Array.BYTES_PER_ELEMENT
Uint16Array.length()The length of the array.\t\n var arrayLength = uint16Array.length
Uint16Array.set(index,value,array,offset)Sets a value or an array of values.\t\n uint16Array.set(index, value);\t\nuint16Array.set(array, offset);
Uint16Array.subarray(newUint16Array,begin,end)Gets a new Uint16Array view of the ArrayBuffer Object store\t\nfor this array, specifying the first and last members of the subarray.\t\n var newUint16Array = uint16Array.subarray(begin, end);
Uint8ClampedArray.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = uint8ClampedArray.buffer
Uint8ClampedArray.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = uint8ClampedArray.byteLength
Uint8ClampedArray.byteOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = uint8ClampedArray.byteOffset
Uint8ClampedArray.BYTES_PER_ELEMENT()The size, in bytes, of each element in the array.\t\n var arraySize = uint8ClampedArray.BYTES_PER_ELEMENT
Uint8ClampedArray.length()The length of the array.\t\n var arrayLength = uint8ClampedArray.length
Uint8ClampedArray.set(index,value,array,offset)Sets a value or an array of values.\t\n uint8ClampedArray.set(index, value);\t\nuint8ClampedArray.set(array, offset);
Uint8ClampedArray.subarray(newUint8ClampedArray,begin,end)Gets a new Uint8ClampedArray view of the ArrayBuffer store\t\nfor this array, specifying the first and last members of the subarray.\t\n var newUint8ClampedArray = uint8ClampedArray.subarray(begin, end);
WeakMap.clear(weakmapObj)Removes all elements from a WeakMap object.\t\n weakmapObj.clear()
WeakMap.constructor()Specifies the function that creates a WeakMap object.\t\n weakmap.constructor
WeakMap.delete(weakmapObj,key)Removes the specified element from a WeakMap object.\t\n weakmapObj.delete(key)
WeakMap.get(weakmapObj,key)Returns a specified element from a WeakMap object.\t\n weakmapObj.get(key)
WeakMap.has(weakmapObj,key)Returns true if the WeakMap object contains the specified element.\t\n weakmapObj.has(key)
WeakMap.prototype()Returns a reference to the prototype for a WeakMap object.\t\n weakmap.prototype
WeakMap.set(weakmapObj,key,value)Adds a new element to a WeakMap object.\t\n weakmapObj.set(key, value)
WeakMap.toString(weakmapObj)Returns a string representation of a WeakMap object.\t\n weakmapObj.toString()
WeakMap.valueOf(weakmapObj)Returns the primitive value of the specified object.\t\n weakmapObj.valueOf()
WinRTError.add(weaksetObj,obj)Adds a new element to a WeakSet.\t\n weaksetObj.add(obj)
WinRTError.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = uint32Array.buffer
WinRTError.buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = uint8Array.buffer
WinRTError.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = uint32Array.byteLength
WinRTError.byteLength()Read-only. The length of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = uint8Array.byteLength
WinRTError.byteOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = uint32Array.byteOffset
WinRTError.byteOffset()Read-only. The offset of this array from the start of its\t\nArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = uint8Array.byteOffset
WinRTError.BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = int8Array.BYTES_PER_ELEMENT
WinRTError.BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = uint32Array.BYTES_PER_ELEMENT
WinRTError.constructor()Specifies the function that creates a WeakSet.\t\n weakset.constructor
WinRTError.delete(weaksetObj,obj)Removes the specified element from a WeakSet.\t\n weaksetObj.delete(obj)
WinRTError.dimensions()Returns the number of dimensions in a VBArray.\t\n array.dimensions( ) 
WinRTError.getItem(safeArray,dimension1,,...,,dimensionN)Returns the item at the specified location.\t\n safeArray.getItem(dimension1[, dimension2, ...], dimensionN) 
WinRTError.has(setObj,obj)Returns true if the WeakSet contains the specified element.\t\n setObj.has(obj)
WinRTError.lbound(safeArray,dimension)Returns the lowest index value used in the specified dimension\t\nof a VBArray.\t\n safeArray.lbound(dimension) 
WinRTError.length()The length of the array.\t\n var arrayLength = uint32Array.length
WinRTError.length()The length of the array.\t\n var arrayLength = uint8Array.length
WinRTError.prototype()Returns a reference to the prototype for a WeakSet.\t\n weakset.prototype
WinRTError.set(index,value,array,offset)Sets a value or an array of values.\t\n uint32Array.set(index, value);\t\nuint32Array.set(array, offset);
WinRTError.set(index,value,array,offset)Sets a value or an array of values.\t\n uint8Array.set(index, value);\t\nuint8Array.set(array, offset);
WinRTError.subarray(newUint32Array,begin,end)Gets a new Uint32Array view of the ArrayBuffer Object store\t\nfor this array, specifying the first and last members of the subarray.\t\n var newUint32Array = uint32Array.subarray(begin, end);
WinRTError.subarray(newUint8Array,begin,end)Gets a new Uint8Array view of the ArrayBuffer Object store\t\nfor this array, specifying the first and last members of the subarray.\t\n var newUint8Array = uint8Array.subarray(begin, end);
WinRTError.toArray()Returns a standard JavaScript array converted from a VBArray.\t\n safeArray.toArray( ) 
WinRTError.ubound(safeArray,dimension)Returns the highest index value used in the specified dimension\t\nof the VBArray.\t\n safeArray.ubound(dimension) 

# Objects with their .Methods
# Javascript functions and Methods
__proto__(object)Contains a reference to the internal prototype of the specified\t\nobject.\t\nobject.__proto__
__proto__(object)Contains a reference to the internal prototype of the specified\t\nobject.\t\nobject.__proto__
{()Returns the characters from the position following the last match to the end of the searched string.\t\nRead-only.
abs()Returns the absolute value of a number (the value without regard to whether it is positive or negative).\t\nFor example, the absolute value of -5 is the same as the absolute value of 5.\t\n Math.abs(number)
acos()Returns the arc cosine (or inverse cosine) of a number.\t\n Math.acos(number)
acosh()Returns the hyperbolic arccosine (or inverse hyperbolic cosine)of a number.\t\n Math.acosh(number)
add(setObj,value)Adds a new element to a set.\t\n setObj.add(value)
add(weaksetObj,obj)Adds a new element to a WeakSet.\t\n weaksetObj.add(obj)
all (func1,func2,funcN)Joins two or more promises and returns only when all the specified\t\npromises have completed or been rejected.\t\n Promise.all(func1, func2 [,funcN])
apply(thisObj,argArray)Calls the function, substituting the specified object for\t\nthe this value of the function, and the specified array for the arguments of the function.\t\n apply([thisObj[,argArray]])
arguments()Gets the arguments for the currently executing Function object.\t\n function.arguments
asin()Returns the arcsine of a number.\t\n Math.asin(number) 
asinh()Returns the inverse hyperbolic sine of a number.\t\n Math.asinh(x) 
assign(target,...sources)Copies the values from one or more source objects to a target\t\nobject.\t\n Object.assign(target, ...sources );
atan()Returns the arctangent of a number.\t\n Math.atan(number) 
atan2(x,y)Returns the angle (in radians) from the X axis to a point (y,x).\t\n Math.atan2(y, x)
atanh()Returns the inverse hyperbolic tangent of a number.\t\n Math.atanh(x) 
atEnd()Returns a Boolean value indicating if the enumerator is at\t\nthe end of the collection.\t\nmyEnum.atEnd()
bind(function,thisArg,arg1[,arg2[,argN]]])For a given function, creates a bound function that has the same body as the original function.\t\nIn the bound function, the this object resolves to the passed in object.\t\nThe bound function has the specified initial parameters.\t\n function.bind(thisArg[,arg1[,arg2[,argN]]])
buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = floatXXArray.buffer
buffer()Read-only. Gets the ArrayBuffer that is referenced by this array.\t\n var arrayBuffer = uint8ClampedArray.buffer
buffer()Read-only. Gets the ArrayBuffer that is referenced by this view.\t\n var arrayBuffer = dataView.buffer
bufferOffset()Read-only. The offset of this array from the start of its ArrayBuffer, in bytes, as fixed at construction time.
byteLength()Read-only. The length of the ArrayBuffer (in bytes).\t\n var arrayLength = arrayBuffer.byteLength
byteLength()Read-only. The length of this array from the start of its ArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayByteLength = float32Array.byteLength
byteOffset()Read-only. The offset of this view from the start of its ArrayBuffer, in bytes, as fixed at construction time.\t\n var arrayOffset = dataView.byteOffset
BYTES_PER_ELEMENT()The size in bytes of each element in the array.\t\n var arraySize = intXXArray.BYTES_PER_ELEMENT
BYTES_PER_ELEMENT()The size, in bytes, of each element in the array.\t\n var arraySize = uintXXClampedArray.BYTES_PER_ELEMENT
call(thisObj,arg1,,arg2,,,,argN)Calls a method of an object, substituting another object for the current object.\t\n call([thisObj[, arg1[, arg2[,  [, argN]]]]])
caller()Gets the function that invoked the current function.\t\n functionName.caller
catch(promise,onRejected)Allows you to specify work to be done on the rejection of a promise.\t\n promise.catch(onRejected)
cbrt()Returns the cube root of a number.\t\n Math.cbrt(number) 
ceil()Returns the smallest integer greater than or equal to its numeric argument.\t\n Math.ceil(number) 
charAt(strObj,index)Returns the character at the specified index.\t\n strObj. charAt(index)
charCodeAt(strObj,index)Returns the Unicode value of the character at the specified location.\t\n strObj. charCodeAt(index)
clear(mapObj)Removes all elements from a map.\t\n mapObj.clear()
clear(setObj)Removes all elements from the set.\t\n setObj.clear()
clear(weakmapObj)Removes all elements from a WeakMap object.\t\n weakmapObj.clear()
clz32()Returns the number of leading zero bits in the 32-bit binary\t\nrepresentation of a number.\t\n Math.clz32(number) 
codePointAt(stringObj,pos)Returns the code point for a Unicode UTF-16 character.\t\n stringObj.codePointAt(pos);
Collator.compare(collatorObj)Returns a function that compares two strings by using the collator’s sort order.\t\n collatorObj.compare
Collator.constructor()Specifies the function that creates a collator.\t\n collator.constructor
Collator.prototype()Returns a reference to the prototype for a collator.\t\n collator.prototype
Collator.resolvedOptions(collatorObj)Returns an object containing the properties and values of the collator object.\t\n collatorObj.resolvedOptions()
Collator(collatorObj,locales,options)Provides locale-specific string comparisons.\t\n collatorObj = new Intl.Collator([locales][, options])
compile(rgExp,pattern,flags)Compiles a regular expression into an internal format for faster execution.\t\n rgExp.compile(pattern, [flags]) 
concat(array1,item1,.,.,.,,itemN,)Combines two or more arrays.\t\n array1.concat([item1[, item2[, . . . [, itemN]]]]) 
concat(string1,,string2,.,.,.,,stringN)Returns a string that contains the concatenation of two or\t\nmore strings.\t\n string1. concat([string2[, string3[, . . . [, stringN]]]])
constructor(boolean,value)Specifies the function that creates an Object.\t\n boolean.constructor([[value])
cos()Returns the cosine of a number.\t\n Math.cos(radians) 
cosh()Returns the hyperbolic cosine of a number.\t\n Math.cosh(x) 
create(prototype,descriptors)Creates an object that has the specified prototype, and that optionally contains specified properties.\t\n Object.create(prototype, descriptors)
DateTimeFormat.constructor()Specifies the function that creates a formatter.\t\n dateTimeFormatter.constructor
DateTimeFormat.format(dateTimeFormatObj)Returns a function that formats a locale-specific date by\t\nusing the specified date/time formatter settings.\t\n dateTimeFormatObj.format
DateTimeFormat.prototype()Returns a reference to the prototype for a formatter.\t\n dateTimeFormat.prototype
DateTimeFormat.resolvedOptions(dateTimeFormatObj)Returns an object that contains the properties and values\t\nof the date/time formatter object.\t\n dateTimeFormatObj.resolvedOptions()
DateTimeFormat(dateTimeFormatObj,locales,options)Provides locale-specific date and time formatting.\t\n dateTimeFormatObj = new Intl.DateTimeFormat([locales][, options])
debuggerEnabled()Determines whether debugging is enabled for the script context.\t\nDebugging may be enabled or disabled whether or not a debugger is attached.\t\n var dbgEnabled = Debug.debuggerEnabled
decodeURI()Gets the unencoded version of an encoded Uniform Resource\t\nIdentifier (URI).\t\n decodeURI(URIstring)
decodeURIComponent()Gets the unencoded version of an encoded component of a Uniform Resource Identifier (URI).\t\n decodeURIComponent(encodedURIString)
defineProperties(object,descriptors)Adds one or more properties to an object, and/or modifies attributes of existing properties.\t\n var newLine = "<br />",\t\n\t\nvar obj = {},\t\nObject.defineProperties(obj, {\t\n  newDataProperty: {\t\n      value: 101,\t\n      writable: true,\t\n      enumerable: true,\t\n      configurable: true\t\n  },\t\n  newAccessorProperty: {\t\n      set: function (x) {\t\n          document.write("in property set accessor" + newLine);\t\n          this.newaccpropvalue = x,\t\n      },\t\n      get: function () {\t\n          document.write("in property get accessor" + newLine);\t\n          return this.newaccpropvalue,\t\n      },\t\n      enumerable: true,\t\n      configurable: true\t\n  }});\t\n\t\n// Set the accessor property value.\t\nobj.newAccessorProperty = 10,\t\ndocument.write ("newAccessorProperty value: " + obj.newAccessorProperty + newLine);\t\n\t\n// Output:\t\n// in property set accessor\t\n// in property get accessor\t\n// newAcc
defineProperty(object,propertyname,descriptor)Adds a property to an object, or modifies attributes of an existing property.\t\n Object.defineProperty(object, propertyname, descriptor)
delete(mapObj,key)Removes the specified element from a map.\t\n mapObj.delete(key)
delete(setObj,value)Removes the specified element from a set.\t\n setObj.delete(value)
delete(weakmapObj,key)Removes the specified element from a WeakMap object.\t\n weakmapObj.delete(key)
delete(weaksetObj,obj)Removes the specified element from a WeakSet.\t\n weaksetObj.delete(obj)
description(object,stringExpression)Returns or sets the descriptive string associated with a specific error.\t\n object.description [= stringExpression]
dimensions()Returns the number of dimensions in a VBArray.\t\n array.dimensions( ) 
endsWith(stringObj,str,position)Returns a value that indicates whether a string or substring ends with another specified string.\t\n stringObj.endsWith(str, [, position]);
entries(arrayObj)Returns an iterator that returns the key/value pairs of the array.\t\n arrayObj.entries();
escape()Encodes strings so they can be read on all computers.\t\nDeprecated.\t\nescape(charString) 
eval(codeString )Evaluates JavaScript code and executes it.\t\n eval(codeString) 
every()Determines whether all the members of an array satisfy the specified test.\t\narray1.every(callbackfn[, thisArg])
exec(rgExp,str)Executes a search on a string using a regular expression pattern,\t\nand returns an array containing the results of that search.\t\n rgExp.exec(str) 
exp()Returns e (the base of natural logarithms) raised to a power.\t\n Math.exp(number) 
expm1()Returns the result of subtracting 1 from e (the base of the\t\nnatural logarithms) raised to a power).\t\n Math.expm1(number) 
fill(arrayObj,value,start,end)Populates an array with a specified value.\t\n arrayObj.fill(value [ , start [ , end ] ]);
filter()Returns the elements of an array that meet the condition specified in a callback function.\t\n array1.filter(callbackfn[, thisArg])
findIndex(arrayObj,callbackfn,thisArg)Returns an index value for the first array\t\nelement that meets ntest criteria specified in a callback function.\t\n arrayObj.findIndex(callbackfn [, thisArg]);
floor()Returns the greatest integer less than or equal to its numeric argument.\t\n Math.floor(number) 
for(key)Returns the symbol for a specified key or, if the key is not present, creates a new symbol object with the new key.\t\n Symbol.for(key);
forEach()Performs the specified action for each element in an array.\t\n array1.forEach(callbackfn[, thisArg])
forEach(mapObj,callbackfn,thisArg)Performs the specified action for each element in a map.\t\n mapObj.forEach(callbackfn[, thisArg])
forEach(setObj,callbackfn,thisArg)Performs the specified action for each element in a set.\t\n setObj.forEach(callbackfn[, thisArg])
freeze(object)Prevents the modification of existing property attributes\t\nand values, and prevents the addition of new properties.\t\n Object.freeze(object)
from(arrayLike,mapfn,thisArg)Returns an array from an array-like or iterable object.\t\n Array.from (arrayLike [ , mapfn [ , thisArg ] ] );
fromCharCode(String,code1,,.,.,.,,,codeN)Returns a string from a number of Unicode character values.\t\n String.fromCharCode([code1[, code2[, ...[, codeN]]]]) 
fromCodePoint(…codePoints)Returns the string associated with a Unicode UTF-16 code point.\t\n String.fromCodePoint(...codePoints);
fround()Returns the nearest single-precision floating-point format\t\nof a number.\t\n Math.fround(number) 
get(mapObj,key)Returns a specified element from a map.\t\n mapObj.get(key)
get(value,index)Omittable. Gets the element at the specified index.\t\n var value = float32Array.get(index);
get(value,index)Omittable. Gets the element at the specified index.\t\n var value = float64Array.get(index);
get(weakmapObj,key)Returns a specified element from a WeakMap object.\t\n weakmapObj.get(key)
getDate()Gets the day-of-the-month, using local time.\t\n dateObj.getDate() 
getDay()Gets the day of the week, using local time.\t\n dateObj. getDay() 
getFloat32(testFloat,byteOffset,littleEndian)Gets the Float32 value at the specified byte offset from the start of the view.\t\n var testFloat = dataView.getFloat32(byteOffset, littleEndian); 
getFloat64(testFloat,byteOffset,littleEndian)Gets the Float64 value at the specified byte offset from the start of the view.\t\n var testFloat = dataView.getFloat64(byteOffset, littleEndian); 
getFullYear()Gets the year, using local time.\t\n dateObj.getFullYear() 
getHours()Gets the hours in a date, using local time.\t\n dateObj.getHours() 
getInt16(testInt,byteOffset,littleEndian)Gets the Int16 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getInt16(byteOffset, littleEndian); 
getInt32(testInt,byteOffset,littleEndian)Gets the Int32 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getInt32(byteOffset, littleEndian); 
getInt8(testInt,byteOffset)Gets the Int8 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getInt8(byteOffset); 
getItem(safeArray,dimension1,,...,,dimensionN)Returns the item at the specified location.\t\n safeArray.getItem(dimension1[, dimension2, ...], dimensionN) 
getMilliseconds()Gets the milliseconds of a Date, using local time.\t\n dateObj.getMilliseconds() 
getMinutes()Gets the minutes of a Date object, using local time.\t\n dateObj.getMinutes() 
getMonth()Gets the month, using local time.\t\n dateObj.getMonth() 
getOwnPropertyDescriptor(object,propertyname)Gets the own property descriptor of the specified object.\t\nAn own property descriptor is one that is defined directly on the object and is not inherited from the object's prototype.\t\n Object.getOwnPropertyDescriptor(object, propertyname)
getOwnPropertyNames()Returns the names of the own properties of an object.\t\nThe own properties of an object are those that are defined directly on that object, and are not inherited from the object's prototype. The properties of an object include both fields (objects) and functions.\t\n Object.getOwnPropertyNames(object)
getOwnPropertySymbols(object)Returns the own symbol properties of an object.\t\nThe own symbol properties of an object are those that are defined directly on that object, and are not inherited from the object's prototype.\t\n Object.getOwnPropertySymbols(object);
getPrototypeOf(object)Returns the prototype of an object.\t\n Object.getPrototypeOf(object)
getSeconds()Gets the seconds of a Date object, using local time.\t\n dateObj.getSeconds() 
getTime()Gets the time value in milliseconds.\t\n dateObj.getTime() 
getTimezoneOffset()Gets the difference in minutes between the time on the local\t\ncomputer and Universal Coordinated Time (UTC).\t\n dateObj.getTimezoneOffset() 
getUint16(testInt,byteOffset,littleEndian)Gets the Uint16 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getUint16(byteOffset, littleEndian); 
getUint32(testInt,byteOffset,littleEndian)Gets the Uint32 value at the specified byte offset from the start of the view.\t\n var req = new XMLHttpRequest();\t\n  req.open('GET', "http://www.example.com");\t\n  req.responseType = "arraybuffer",\t\n  req.send();\t\n\t\n  req.onreadystatechange = function () {\t\n      if (req.readyState === 4) {\t\n          var buffer = req.response,\t\n          var dataView = new DataView(buffer);\t\n          alert(dataView.getUint32(0));\t\n      }\t\n}
getUint8(testInt,byteOffset)Gets the Uint8 value at the specified byte offset from the start of the view.\t\n var testInt = dataView.getUint8(byteOffset); 
getUTCDate()Gets the day-of-the-month, using Universal Coordinated Time (UTC).\t\n dateObj.getUTCDate() 
getUTCDay()Gets the day of the week using Universal Coordinated Time (UTC).\t\n dateObj.getUTCDay() 
getUTCFullYear()Gets the year using Universal Coordinated Time (UTC).\t\n dateObj.getUTCFullYear() 
getUTCHours()Gets the hours value in a Date object using Universal Coordinated Time (UTC).\t\n dateObj.getUTCHours() 
getUTCMilliseconds()Gets the milliseconds of a Date object using Universal Coordinated Time (UTC).\t\n dateObj.getUTCMilliseconds() 
getUTCMinutes()Gets the minutes of a Date object using Universal Coordinated Time (UTC).\t\n dateObj.getUTCMinutes() 
getUTCMonth()Gets the month of a Date object using Universal Coordinated Time (UTC).\t\n dateObj.getUTCMonth() 
getUTCSeconds()Gets the seconds of a Date object using Universal Coordinated Time (UTC).\t\n dateObj.getUTCSeconds() 
getVarDate()Returns a VT_DATE value from a Date object.\t\ndateObj.getVarDate() 
getYear()Gets the year of a Date object.\t\ndateObj.getYear() 
global()Returns a Boolean value indicating the state of the global flag (g) used with a regular expression.\t\nDefault is false. Read-only.\t\n rgExp.global
has(mapObj,key)Returns true if the map contains the specified element.\t\n mapObj.has(key)
has(setObj,obj)Returns true if the WeakSet contains the specified element.\t\n setObj.has(obj)
has(setObj,value)Returns true if the set contains the specified element.\t\n setObj.has(value)
has(weakmapObj,key)Returns true if the WeakMap object contains the specified element.\t\n weakmapObj.has(key)
hasOwnProperty(object,proName)Determines whether an object has a property with the specified name.\t\n object.hasOwnProperty(proName)
HTML()You can use HTML tag methods to place HTML elements around text in a String object.
hypot(value1,value2,values)Returns the square root of the sum of the squares of the arguments.\t\n Math.hypot ( value1[, value2[, ...values] );
ignoreCase()Returns a Boolean value indicating the state of the ignoreCase flag (i) used with a regular expression.\t\nDefault is false. Read-only.\t\n rgExp.ignoreCase
imul(x,y)Returns the product of two numbers that are treated as 32-bit signed integers.\t\n Math.imul(x, y);
includes(stringObj,substring,position)Returns a Boolean value that indicates whether the passed in string is contained in the string object.\t\n stringObj.includes(substring [, position]);
index()Returns the character position where the first successful match begins in a searched string.-Read-only.\t\n RegExp.index 
indexOf()Returns the index of the first occurrence of a value in an array.\t\n array1.indexOf(searchElement[, fromIndex])
indexOf(strObj,subString,startIndex)Returns the position of the first occurrence of a substring.\t\n strObj. indexOf(subString[, startIndex])
input()Returns the string against which a regular expression search was performed.-Read-only.\t\n RegExp.input
is(value1,value2)Returns a value that indicates whether two values are the same value.\t\n Object.is(value1, value2)
isArray(object)Determines whether an object is an array.\t\n Array.isArray(object) 
isExtensible(object)Returns a value that indicates whether new properties can be added to an object.\t\n Object.isExtensible(object)
isFinite()Returns a Boolean value that indicates whether a value is a finite number.\t\n Number.isFinite(numValue)
isFrozen(object)Returns true if existing property attributes and values cannot be modified in an object,\t\nand new properties cannot be added to the object.\t\n Object.isFrozen(object)
isInteger()Returns a Boolean value that indicates whether a value is an integer.\t\n Number.isInteger(numValue)
isNaN(numValue) Returns a Boolean value that indicates whether a value is the reserved value NaN (not a number).\t\n Number.isNaN(numValue)
isPrototypeOf(prototype,object)Determines whether an object exists in another object's prototype\t\nchain.\t\n prototype.isPrototypeOf(object)
isSafeInteger()Returns a Boolean value that indicates whether a number can be safely represented in JavaScript.\t\n Number.isSafeInteger(numValue) 
isSealed(object)Returns true if existing property attributes cannot be modified in an object and new properties cannot be added to the object.\t\n Object.isSealed(object)
isView(object)Determines whether an object provides a view of the buffer.\t\n ArrayBuffer.isView(object)
item()Returns the current item in the collection.\t\nenumObj.item() 
join(arrayObj,separator)Adds all the elements of an array separated by the specified separator string.\t\n arrayObj.join([separator]) 
keyFor(sym)Returns the key for a specified symbol.\t\n Symbol.keyFor(sym);
keys()Returns the names of the enumerable properties and methods of an object.\t\n Object.keys(object)
keys(arrayObj)Returns an iterator that returns the index values of the array.\t\n arrayObj.keys();
lastIndex()Returns the character position where the next match begins in a searched string.\t\n RegExp.lastIndex
lastIndexOf()Returns the index of the last occurrence of a specified value in an array.\t\n array1.lastIndexOf(searchElement[, fromIndex])
lastIndexOf(strObj,substring,startindex)Returns the last occurrence of a substring in the string.\t\n strObj.lastIndexOf(substring[, startindex])
lastMatch()Returns the last matched characters from any regular expression search.-Read-only.\t\n RegExp.lastMatch
lastParen()Returns the last parenthesized submatch from any regular expression search, if any.-Read-only.\t\n RegExp.lastParen
lbound(safeArray,dimension)Returns the lowest index value used in the specified dimension\t\nof a VBArray.\t\n safeArray.lbound(dimension) 
leftContext()Returns the characters from the beginning of a searched string up to the position before the beginning of the last match.-Read-only.\t\n RegExp.leftContext
length()Gets the number of arguments defined for a function.\t\n functionName.length
length()Returns the length of a String object.\t\nstrVariable.length\t\n"String Literal".length 
length()The length of the array.\t\n var arrayLength = uintXXArray.length
length()The length of the array.\t\n var arrayLength = uint8ClampedArray.length
length(numVar,arrayObj)Gets or sets the length of the array. This is a number one higher than the highest element defined in an array.\t\nnumVar = arrayObj.length 
localeCompare(stringVar,stringExp,locales,options)Determines whether two strings are equivalent in the current\t\nlocale.\t\nstringVar.localeCompare(stringExp[, locales][, options]) 
log(number)Returns the natural logarithm (base e) of a number.\t\n Math.log(number) 
log10()Returns the base 10 logarithm of a number.\t\n Math.log10(x) 
log1p()Returns the natural logarithm of 1 + a number.\t\n Math.log1p(x) 
log2()Returns the base 2 logarithm of a number.\t\n Math.log2(x) 
map()Calls a defined callback function on each element of an array,\t\nand returns an array that contains the results.\t\n array1.map(callbackfn[, thisArg])
match(stringObj,rgExp)Matches a string with a regular expression, and returns an\t\narray containing the results of that search.\t\n stringObj.match(rgExp) 
max()Returns the larger of a set of supplied numeric expressions.\t\n Math.max([number1[, number2[... [, numberN]]]]) 
message(errorObj)Returns an error message string.\t\n errorObj.message
min()Returns the smaller of a set of numeric expressions.\t\n Math.min([number1[, number2[... [,numberN]]]])
moveFirst()Resets the current item in the collection to the first item.\t\n enumObj.moveFirst( ) 
moveNext()Moves the current item to the next item in the collection.\t\n enumObj.moveNext( ) 
msTraceAsyncCallbackCompleted()Indicates that the callback stack associated with a previously\t\nspecified asynchronous operation has completed.\t\nDebug.msTraceAsyncCallbackCompleted()
msTraceAsyncCallbackStarting(asyncOperationId)Associates the callback stack with a previously specified asynchronous operation.\t\nDebug.msTraceAsyncCallbackStarting(asyncOperationId)
msTraceAsyncOperationCompleted(asyncOperationId,status)Indicates that an asynchronous operation has completed.\t\nDebug.msTraceAsyncOperationCompleted(asyncOperationId, status)
msTraceAsyncOperationStarting(operationName)Initiates a trace for an asynchronous operation.\t\nDebug.msTraceAsyncOperationStarting(operationName)
msUpdateAsyncCallbackRelation(relatedAsyncOperationId,relationType)Updates the relationship status between a synchronous work\t\nitem and the associated asynchronous operation.\t\nDebug.msUpdateAsyncCallbackRelation(relatedAsyncOperationId, relationType)
multiline()Returns a Boolean value indicating the state of the multiline flag (m) used with a regular expression.\t\nDefault is false. Read-only.\t\n rgExp.multiline
name(errorObj)Returns the name of an error.\t\n errorObj.name
normalize(stringObj,form)Returns the Unicode Normalization Form of a specified string.\t\n stringObj.normalize([form]);
now()Gets the current date and time.\t\n Date.now()
number(Object,errorNumber)Returns or sets the numeric value associated with a specific error.\t\nThe Error object's default property is number.\t\n object.number [= errorNumber]
NumberFormat.constructor()Specifies the function that creates a formatter.\t\n numberFormatter.constructor
NumberFormat.format(numberFormatObj)Returns a function that formats a locale-specific number by using the specified number formatter settings.\t\n numberFormatObj.format
NumberFormat.prototype()Returns a reference to the prototype for a formatter.\t\n numberFormat.prototype
NumberFormat.resolvedOptions(numberFormatObj)Returns an object that contains the properties and values\t\nof the NumberFormat formatter object.\t\n numberFormatObj.resolvedOptions()
NumberFormat(numberFormatObj,locales,options)Provides locale-specific number formatting.\t\n numberFormatObj = new Intl.NumberFormat([locales][, options])
of(element0,...,elementN)Returns an array from the passed in arguments.\t\n Array.of(element0[, element1][, ...][,elementN]);
parse()Parses a string containing a date, and returns the number\t\nof milliseconds between that date and midnight, January 1, 1970.\t\n Date.parse(dateVal) 
parse(text,reviver)Converts a JavaScript Object Notation (JSON) string into an object.\t\n JSON.parse(text [, reviver])
parseFloat()Converts a string to a floating-point number.\t\n parseFloat(numString) 
parseInt(numString,radix)Converts a string to an integer.\t\n parseInt(numString, [radix]) 
pop()Removes the last element from an array and returns it.\t\n arrayObj.pop()
pow(base,exponent)Returns the value of a base expression taken to a specified power.\t\n Math.pow(base, exponent) 
preventExtensions(object)Prevents the addition of new properties to an object.\t\n Object.preventExtensions(object)
propertyIsEnumerable(object,proName)Determines whether a specified property is enumerable.\t\n object.propertyIsEnumerable(proName)
prototype()Returns a reference to the prototype for a class.\t\n array.prototype
push(arrayObj,item,,item2,.,.,.,,itemN)Appends new elements to an array, and returns the new length\t\nof the array.\t\n arrayObj.push([item1 [item2 [. . . [itemN ]]]])
race(iterable)Creates a new promise that will resolve or reject with the\t\nsame result value as the first promise to resolve or reject among the passed in arguments.\t\n Promise.race(iterable)
random()Returns a pseudorandom number between 0 and 1.\t\n Math.random( ) 
raw(templateStr,obj,...substitutions)Returns the raw string form of a template string.\t\n String.raw`templateStr`,\t\nString.raw(obj, ...substitutions);
reduce()Calls the specified callback function for all the elements in an array.\t\nThe return value of the callback function is the accumulated result, and is provided as an argument in the next call to the callback function.\t\n array1.reduce(callbackfn[, initialValue])
reduceRight()Calls the specified callback function for all the elements in an array, in descending order.\t\nThe return value of the callback functionis the accumulated result, and is provided as an argument in the next call to the callback function.\t\n array1.reduceRight(callbackfn[, initialValue])
reject(r)Creates a new rejected promise with a result equal to the passed in argument.\t\n Promise.reject(r);
repeat(stringObj,count)Returns a new string object with a value equal to the original\t\nstring repeated the specified number of times.\t\n stringObj.repeat(count);
replace(stringObj,,rgExp,,replaceText,)Replaces text in a string, using a regular expression or search\t\nstring.\t\n stringObj. replace(rgExp, replaceText)
resolve(x)Creates a new resolved promise with a result equal to its argument.\t\n Promise.resolve(x)
reverse(arrayObj)Reverses the elements in an Array.\t\n arrayObj.reverse() 
round()Returns a supplied numeric expression rounded to the nearest\t\ninteger.\t\n Math.round(number) 
ScriptEngine()Gets the name of the scripting language in use.\t\n ScriptEngine()
ScriptEngineBuildVersion()Gets the build version number of the scripting engine in use.\t\n ScriptEngineBuildVersion()
ScriptEngineMajorVersion()Gets the major version number of the scripting engine in use.\t\n ScriptEngineMajorVersion()
ScriptEngineMinorVersion()Gets the minor version number of the scripting engine in use.\t\n ScriptEngineMinorVersion()
seal(object)Prevents the modification of attributes of existing properties,\t\nand prevents the addition of new properties.\t\n Object.seal(object)
search(stringObj,rgExp)Finds the first substring match in a regular expression search.\t\n stringObj.search(rgExp) 
set(index,value,array,offset)Sets a value or an array of values.\t\n floatXXArray.set(index, value);\t\n float32Array.set(array, offset);
set(index,value,array,offset)Sets a value or an array of values.\t\n uint8ClampedArray.set(index, value);\t\n uint8ClampedArray.set(array, offset);
set(mapObj,key,value)Adds a new element to a map.\t\n mapObj.set(key, value)
set(weakmapObj,key,value)Adds a new element to a WeakMap object.\t\n weakmapObj.set(key, value)
setDate(dateObj,numDate)Sets the numeric day-of-the-month value of the Date object using local time.\t\n dateObj.setDate(numDate) 
setFloat32(byteOffset,value,littleEndian)Sets the Float32 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setFloat32 (byteOffset, value, littleEndian); 
setFloat64(byteOffset,value,littleEndian)Sets the Float64 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setFloat64 (byteOffset, value, littleEndian); 
setFullYear(dateObj,,numYear,numMonth,numDate)Sets the year of the Date object using local time.\t\n dateObj.setFullYear(numYear[, numMonth[, numDate]]) 
setHours(dateObj,numHours,numMin,numSec,numMilli)Sets the hour value in the Date object using local time.\t\n dateObj.setHours(numHours[, numMin[, numSec[, numMilli]]]) 
setInt16(byteOffset,value,littleEndian)Sets the Int16 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setInt16(byteOffset, value, littleEndian); 
setInt32(byteOffset,value,littleEndian)Sets the Int32 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setInt32 (byteOffset, value, littleEndian); 
setInt8(byteOffset,value)Stores an Int8 value at the specified byte offset from the start of the view.\t\n dataView.setInt8(byteOffset, value); 
setMilliseconds(dateObj,numMilli)Sets the milliseconds value in the Date object using local time.\t\n dateObj.setMilliseconds(numMilli) 
setMinutes(dateObj,numMinutes,numSeconds,numMilli)Sets the minutes value in the Date object using local time.\t\n dateObj.setMinutes(numMinutes[, numSeconds[, numMilli]]) 
setMonth(dateObj,numMonth,dateVal)Sets the month value in the Date object using local time.\t\n dateObj. setMonth(numMonth[, dateVal]) 
setNonUserCodeExceptions()Determines whether any try-catch blocks in this scope are to be treated by the debugger as user-unhandled.\t\nExceptions can be classified as thrown, user-unhandled or unhandled.\t\n Debug.setNonUserCodeExceptions [= bool]
setPrototypeOf(obj,proto)Sets the prototype of an object.\t\nObject.setPrototypeOf(obj, proto);
setSeconds(dateObj,numSeconds,numMilli)Sets the seconds value in the Date object using local time.\t\n dateObj.setSeconds(numSeconds[, numMilli]) 
setTime(dateObj,milliseconds)Sets the date and time value in the Date object.\t\n dateObj.setTime(milliseconds) 
setUint16(testInt,value,byteOffset,littleEndian)Sets the Uint16 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setUint16(byteOffset, value, littleEndian); 
setUint32(byteOffset,value,littleEndian)Sets the Uint32 value at the specified byte offset from the start of the view.\t\nThere is no alignment constraint, multi-byte values may be set at any offset.\t\n dataView.setUint32 (byteOffset, value, littleEndian); 
setUint8(byteOffset,value)Stores a Uint8 value at the specified byte offset from the start of the view.\t\n dataView.setUint8(byteOffset, value); 
setUTCDate(dateObj,numDate)Sets the numeric day of the month in the Date object using Universal Coordinated Time (UTC).\t\n dateObj.setUTCDate(numDate) 
setUTCFullYear(dateObj,numYear,numMonth,numDate)Sets the year value in the Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.setUTCFullYear(numYear[, numMonth[, numDate]]) 
setUTCHours(dateObj,numHours,numMin,numSec,numMilli)Sets the hours value in the Date object using Universal Coordinated\t\nTime (UTC).\t\n dateObj.setUTCHours(numHours[, numMin[, numSec[, numMilli]]]) 
setUTCMilliseconds(dateObj,numMilli)Sets the milliseconds value in the Date object using Universal Coordinated Time (UTC).\t\n dateObj.setUTCMilliseconds(numMilli) 
setUTCMinutes(dateObj,numMinutes,numSeconds,numMilli)Sets the minutes value in the Date object using Universal Coordinated Time (UTC).\t\n dateObj.setUTCMinutes(numMinutes[, numSeconds[, numMilli]]) 
setUTCMonth(dateObj,numMonth,dateVal)Sets the month value in the Date object using Universal Coordinated Time (UTC).\t\n dateObj.setUTCMonth(numMonth[, dateVal]) 
setUTCSeconds(dateObj,numSeconds,numMilli)Sets the seconds value in the Date object using Universal Coordinated Time (UTC).\t\n dateObj.setUTCSeconds(numSeconds[, numMilli]) 
setYear(dateObj,numYear)Sets the year value in the Date object.\t\ndateObj.setYear(numYear) 
shift()Removes the first element from an array and returns it.\t\n arrayObj.shift()
sign()Returns the sign of a number, indicating whether the number is positive, negative, or 0.\t\n Math.sign(number)
sin()Returns the sine of a number.\t\n Math.sin(radians) 
sinh()Returns the inverse hyperbolic sine of a number.\t\n Math.sinh(x) 
size(sizeVar,mapObj)Returns the number of elements in a map.\t\n sizeVar = mapObj.size
size(sizeVar,setObj)Returns the number of elements in a set.\t\n sizeVar = setObj.size
slice(arrayBufferObj,start,,end,)Returns a section of an ArrayBuffer.\t\n arrayBufferObj.slice(start, [end]) 
slice(arrayObj,start,,end,)Returns a section of an array.\t\n arrayObj.slice(start, [end]) 
slice(stringObj,start,,end,)Returns a section of a string.\t\n stringObj.slice(start, [end]) 
some()Determines whether the specified callback function returns true for any element of an array.\t\narray1.some(callbackfn[, thisArg])
sort(arrayObj,sortFunction)Sorts an Array.\t\n arrayobj.sort(sortFunction) 
source()Returns a copy of the text of the regular expression pattern.\t\nRead-only. Argument is a Regular expression object.\t\n rgExp.source
splice(arrayObj,start,deleteCount,item1,,item2,.,.,.,,itemN)Removes elements from an array and, if necessary, inserts new elements in their place, returning the deleted elements.\t\n arrayObj.splice(start, deleteCount, [item1[, item2[, . . . [,itemN]]]])
split(stringObj,,separator,,limit)Split a string into substrings using the specified separator and return them as an array.\t\n stringObj.split([separator[, limit]])
sqrt()Returns the square root of a number.\t\n Math.sqrt(number) 
stack()Gets or sets the error stack as a string that contains the stack trace frames.\t\n object.stack 
stackTraceLimit()Gets or sets the stack trace limit, the default limit is 10.\t\n Error.stackTraceLimit 
startsWith(stringObj,str,position)Returns a value that indicates whether a string or substring starts with another specified string.\t\n stringObj.startsWith(str, [, position]);
stringify(value,replacer,space)Converts a JavaScript value to a JavaScript Object Notation (JSON) string.\t\n JSON.stringify(value [, replacer] [, space])
subarray(Array,begin,end)Gets a new Array of the ArrayBuffer Object store for this array, specifying the first and last members of the subarray.\t\n var newUint8Array = uint8Array.subarray(begin, end);
substr(stringvar,start,length)Gets a substring beginning at the specified location and having the specified length.\t\n stringvar.substr(start [, length ]) 
substring(start,end)Returns the substring at the specified location within a String object.\t\n"String Literal".substring(start [, end]) 
tan()Returns the tangent of a number.\t\n Math.tan(radians) 
tanh()Returns the hyperbolic tangent of a number.\t\n Math.tanh(x)
test(rgExp,str)Returns a Boolean value that indicates whether or not a pattern exists in a searched string.\t\n rgExp.test(str) 
then(promise,onCompleted,onRejected)Allows you to specify work to be done on the fulfillment of a promise.\t\n promise.then(onCompleted, onRejected);
toArray()Returns a standard JavaScript array converted from a VBArray.\t\n safeArray.toArray( ) 
toDateString()Returns a date as a string value.\t\n objDate.toDateString()
toExponential(numObj,fractionDigits)Represents a number in exponential notation.\t\n numObj. toExponential([fractionDigits])
toFixed(numObj,fractionDigits)Represents a number in fixed-point notation.\t\n numObj.toFixed([fractionDigits])
toGMTString()Returns a date converted to a string using Greenwich Mean Time(GMT).\t\n dateObj.toGMTString() 
toISOString()Returns a date as a string value in ISO format.\t\n objDate.toISOString()
toJSON(objectname)Used by the JSON.stringify method to enable the transformation of an object's data to (JSON) serialization.\t\n objectname.toJSON()
toLocaleDateString(dateObj,locales,options)Returns a date as a string value that is appropriate to the host environment's current locale or the specified locale.\t\ndateObj.toLocaleDateString( [locales][, options]) 
toLocaleLowerCase()Converts all alphabetic characters to lowercase\t\n stringVar.toLocaleLowerCase()
toLocaleString()Returns a date converted to a string using the current locale.\t\n dateObj.toLocaleString() 
toLocaleString(dateObj,locales,options)Converts a date to a string by using the current or specified locale.\t\n dateObj.toLocaleString([locales][, options]) 
toLocaleString(numberObj,locales,options)Converts a number to a string by using the current or specified locale.\t\n numberObj.toLocaleString([locales][, options]) 
toLocaleTimeString(dateObj,locales,options)Returns a time as a string value that is appropriate to the host environment's current locale.\t\ndateObj.toLocaleTimeString([locales][, options])
toLocaleUpperCase()Returns a string where all alphabetic characters have been converted to uppercase\t\n stringVar.toLocaleUpperCase()
toLowerCase()Converts all the alphabetic characters in a string to lowercase.\t\n"String Literal".toLowerCase() 
toPrecision(numObj,precision)Represents a number either in exponential or fixed-point notation with a specified number of digits.\t\n numObj.toPrecision([precision])
toString(array)Returns a string representation of an array.\t\n array.toString()
toString(number)Returns a string representation of a number.\t\n number.toString()
toString(objectname,radix)Returns a string representation of an object.\t\n objectname.toString([radix])
toTimeString()Returns a time as a string value.\t\n objDate. toTimeString()
toUpperCase()Converts all the alphabetic characters in a string to uppercase.\t\n"String Literal".toUpperCase() 
toUTCString()Returns a date converted to a string using Universal Coordinated Time (UTC).\t\n dateObj.toUTCString() 
trim(stringObj )Removes the leading and trailing white space and line terminator characters from a string.\t\n stringObj.trim()
trunc()Returns the integer portion of a number, removing any fractional digits.\t\n Math.trunc(number)
ubound(safeArray,dimension)Returns the highest index value used in the specified dimension of the VBArray.\t\n safeArray.ubound(dimension) 
undefined()A value that has never been defined, such as a variable that has not been initialized.\t\n undefined
unescape()Decodes String objects encoded with the escape function.\t\nDeprecated.\t\nunescape(charString) 
unshift(arrayObj,item1,,item2,.,.,.,,itemN)Inserts new elements at the start of an array.\t\n arrayObj.unshift([item1[, item2 [, . . . [, itemN]]]])
UTC(year,month,day,hours,minutes,seconds,ms)Returns the number of milliseconds between midnight, January\t\n1, 1970 Universal Coordinated Time (UTC) (or GMT) and the specified date.\t\n Date.UTC(year, month, day[, hours[, minutes[, seconds[,ms]]]]) 
valueOf()Returns the primitive value of the specified number.\t\n number.valueOf()
valueOf()Returns the stored time value in milliseconds since midnight,\t\nJanuary 1, 1970 UTC.\t\n date.valueOf()
valueOf()Returns the string.\t\n string.valueOf()
valueOf(mapObj)Returns the primitive value of the specified object.\t\n mapObj.valueOf()
valueOf(setObj)Returns the primitive value of the specified object.\t\n setObj.valueOf()
valueOf(weakmapObj)Returns the primitive value of the specified object.\t\n weakmapObj.valueOf()
values(arrayObj)Returns an iterator that returns the values of the array.\t\n arrayObj.values();
write(str1, str2, ... , strN)Sends strings to the script debugger.\t\nDebug.write([str1 [, str2 [, ... [, strN]]]])
writeln(str1, str2, ... , strN)Sends strings to the script debugger, followed by a newline character.\t\nDebug.writeln([str1 [, str2 [, ... [, strN]]]])


# Javascript Constants
Math.E()The mathematical constant e. This is Euler's number, the base of natural logarithms. (e~2718)
Math.LN10()The natural logarithm of 10. (ln10~2302)
Math.LN2()The natural logarithm of 2. (ln2~693)
Math.LOG10E()The base-10 logarithm of e. (lg10e~434)
Math.LOG2E()The base-2 logarithm of e. (lg2e~1443)
Math.PI()Pi. This is the ratio of the circumference of a circle to its diameter. (pi~3.14159)
Math.SQRT1_2()The square root of 0.5, or, equivalently, one divided by the square root of 2. (sqrt1/2~707)
Math.SQRT2()The square root of 2. (sqrt2~1414)
Number.EPSILON()The smallest number that can be represented in JavaScript. Equal to approximately 2.2204460492503130808472633361816E-16. (~)
Number.MAX_SAFE_INTEGER()The largest number that can be safely represented in JavaScript. Equal to 9007199254740991. (~)
Number.MAX_VALUE()The largest number that can be represented in JavaScript. Equal to approximately 1.79E+308. (~)
Number.MIN_SAFE_INTEGER()The smallest number that can be safely represented in JavaScript. Equal to ?9007199254740991. (~)
Number.MIN_VALUE()The closest number to zero that can be represented in JavaScript. Equal to approximately 5.00E-324. (~)
Number.NEGATIVE_INFINITY()A value that is less than the largest negative number that can be represented in JavaScript.
Number.POSITIVE_INFINITY()A value greater than the largest number that can be represented in JavaScript. \t\nJavaScript displays POSITIVE_INFINITY values as infinity.  (~)

E()The mathematical constant e. This is Euler's number, the base of natural logarithms. (e~2718)
EPSILON()The smallest number that can be represented in JavaScript. Equal to approximately 2.2204460492503130808472633361816E-16. (~)
Infinity()A number that is larger than the largest floating point number.\t\nReturns an initial value of Number.
LN10()The natural logarithm of 10. (ln10~2302)
LN2()The natural logarithm of 2. (ln2~693)
LOG10E()The base-10 logarithm of e. (lg10e~434)
LOG2E()The base-2 logarithm of e. (lg2e~1443)
MAX_SAFE_INTEGER()The largest number that can be safely represented in JavaScript. Equal to 9007199254740991. (~)
MAX_VALUE()The largest number that can be represented in JavaScript. Equal to approximately 1.79E+308. (~)
MIN_SAFE_INTEGER()The smallest number that can be safely represented in JavaScript. Equal to ?9007199254740991. (~)
MIN_VALUE()The closest number to zero that can be represented in JavaScript. Equal to approximately 5.00E-324. (~)
NaN()A value that is not a number. In equality comparisons, NaN does not equal any value, including itself.\t\n See use of the isNaN function.
NEGATIVE_INFINITY()A value that is less than the largest negative number that can be represented in JavaScript.\t\nJavaScript displays NEGATIVE_INFINITY values as -infinity.  (~)
null()Null:)
PI()Pi. This is the ratio of the circumference of a circle to its diameter. (pi~3.14159)
POSITIVE_INFINITY()A value greater than the largest number that can be represented in JavaScript. \t\nJavaScript displays POSITIVE_INFINITY values as infinity.  (~)
SQRT1_2()The square root of 0.5, or, equivalently, one divided by the square root of 2. (sqrt1/2~707)
SQRT2()The square root of 2. (sqrt2~1414)

# Javascript Keywords

abstract
break
byte
case
catch
char
class
const
continue
debugger
default
do
double
else
enum
export
extends
false
final
finally
float
for
function
goto
if
implements
import
in
instanceof
int
interface
let
long
native
new
null
package
private
protected
public
return
short
static
super
switch
synchronized
this
throw
throws
transient
true
try
typeof
var
void
volatile
while
with
yield
	
